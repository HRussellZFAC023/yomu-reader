# P0 Backlog - 2026-06-14

All items below are P0. Each worker thread should own one item end to end: reproduce, fix, test, update release notes, build/sync generated assets when needed, push to `main`, and publish or trigger the next release. Before pushing, pull/rebase `main` and choose the next patch version if another worker has released first.

## Common Worker Contract

- Work in `/Users/heru/Documents/Projects/yomu/apps/yomu-reader`.
- Use the Yomu audit workflow: inspect `git status --short`, `AGENTS.md`, `package.json`, generated asset conventions, and nearby code before editing.
- Preserve unrelated user changes. Do not revert work from other threads.
- Prefer source fixes with focused tests over hardcoded symptom patches.
- For user-visible behavior, update `CHANGELOG.md` and any relevant docs/i18n copy.
- Run focused tests first, then the smallest credible gate. Final release candidates should run `npm run check`; broad scanner/parser changes should also run `npm run fallow:dead-code` and `npm run fallow:audit`.
- If source changes affect the userscript/newtab/docs assets, run `npm run build`, `node scripts/sync-docs-userscript.cjs`, `npm run docs:build`, and `npm run verify`.
- Integrate the finished work back into `main`. Do not stop at a local branch or worktree. Rebase on current `origin/main`, resolve conflicts carefully, and push with `git push origin HEAD:main` or an equivalent reviewed flow.
- If `main` has advanced, or another worker already fixed part of the issue, merge the implementations thoughtfully. Do not blindly accept remote changes or local changes. Read both sides, preserve the best behavior from each, and keep/adjust tests so the combined result is verified.
- Commit source/release metadata separately from generated assets when practical, but make sure the final pushed tip on `main` is not a `[skip ci]`-only generated-assets commit. CI and Pages deploy must run.
- Publish a release for user-facing fixes: create/push the next `v*` tag or run the Release workflow, then verify with `gh release view <tag>` that the release exists, is non-draft, and includes the built userscript asset.
- Do not mark a P0 fixed from commits, unit tests, or smoke scripts alone. For every user-facing item, include evidence for the exact reported journey: the reported URL/page, the relevant settings state, before/after DOM or screenshot evidence, and a live-hosted or browser-backed verification after the release deploys.
- If a previous backlog row says `verified fixed` but fresh user evidence shows the bug still reproduces, treat the fresh live evidence as authoritative, reopen the item, and route the fix immediately.
- A release is not the finish line. The finish line is the user-visible behavior working on the hosted/newtab/userscript journey that was originally reported.

## P0-01 Universal Active/Passive Scanning And Popover Parsing

Source evidence:
- Attachment: `Task Prompt - Yomu Reader: universal active/passive scan model + popover parsing`.
- User reports passive links on JPDB do not open hover popovers.
- User reports settings tab buttons have the opposite problem where native tab/button behavior is disrupted.
- Buttons like `data-action="copy-newtab-url"` should be passive: hover lookup works, click remains native.
- Text such as `設定を検索`, settings menu labels, docs chrome, and newtab chrome should get ruby/pitch where safe.
- `<summary class="jpdb-reader-local-title">翻訳</summary>` in the popover should be parsed visually but must still toggle the details element and must not open nested popovers.

Acceptance:
- Replace per-site semantic exclude lists with one active/passive model.
- Interactive ancestors classify as passive, not excluded.
- Passive words render color, pitch underline, and ruby where safe.
- Click/tap on passive words propagates to host controls.
- Hover opens lookup outside the dictionary popover only.
- Words inside the dictionary popover are render-only and cannot open popover-in-popover.
- Settings tab navigation, buttons, links, and summary toggles all work when parsed.
- Keep the YouTube virtualized-title ruby guard intact while allowing stable headings/buttons to get ruby.

## P0-02 Japanese Parser Correctness And Sentence Context

Source evidence:
- Attachment: `ややさしい is being missparsed as やや さし`.
- User asks to pick up the Japanese parsing story and avoid frequent misparsing without hardcoding.
- Selection parsing screenshot shows `好きなものを読む日本語を学ぶ` split into overly isolated tiles.
- Selection popup is missing for large sentences.

Acceptance:
- Parser uses sentence context to choose coherent words from continuous Japanese text.
- Add regression tests for `ややさしい`, `好きなものを読んで日本語を学ぶ`, `読み取る`, and long selected sentences.
- Do not hardcode exact outputs for one page; improve ranking/coverage behavior.
- Selection popup appears for large sentences and preserves sentence-context parses.
- Local, JPDB, Jiten, and fallback parses do not leave suspicious dangling kana stems when better context exists.

## P0-03 Immersion Kit Rendering And Controls

Source evidence:
- Newtab URL: `https://hrussellzfac023.github.io/yomu-reader/newtab/index.html?q=%E3%82%88%E3%82%80`.
- Newtab URL: `https://hrussellzfac023.github.io/yomu-reader/newtab/index.html?q=%E8%AA%AD%E3%81%BF%E5%8F%96%E3%82%8B`.
- Attachment: Immersion Kit HTML source card.
- Images 1 and 2 show CSS differences between newtab/docs injection and JPDB injection.
- User reports audio, next, and previous controls do not work.
- User reports strange Immersion Kit behavior for `読み取る`.

Acceptance:
- Immersion Kit cards look consistent across JPDB popover, newtab search, and hosted docs.
- Audio button plays the current example, with fallbacks for blocked/missing URLs.
- Next/previous controls advance examples and update count, sentence, media, translation, audio URLs, and parsed text.
- CSS layout avoids the oversized floating subtitle box seen in Image 1.
- Add unit or DOM tests for card state updates plus a browser smoke for newtab and popover.

## P0-04 Newtab Search, Query Params, And Search Result Actions

Source evidence:
- User reports changing the search term or selecting a word inside a modal should update the `q` query param.
- User says redundant global external search links should be removed because each dictionary entry has pills.
- User expects Jiten and JPDB entry pills, Copy, Yomu, and Anki actions to work.
- User reports Anki pill does nothing.

Acceptance:
- Newtab search form and modal-driven searches update browser history/query params consistently.
- Back/forward restores searched term and rendered results.
- Remove redundant global `jpdb-reader-newtab-search-links` where entry-level pills already exist.
- Entry pills respect settings, are visible in selection popovers, and perform their actions.
- Anki pill gives visible success/error feedback and works in newtab and popover flows.

## P0-05 Jiten, JPDB, Local Dictionaries, And Store Coherence

Source evidence:
- User reports Jiten is missing altogether on newtab, despite Jiten and JPDB enabled, and no API keys should be needed.
- Local dictionaries work on normal pages but not docs pages, even when userscript is on.
- Docs page and userscript stores appear to conflict.
- Installing a dictionary on newtab gets stuck on `Downloading: jitendex-yomitan.zip...`.
- Yomitan dictionaries are not showing on the search page.

Acceptance:
- Jiten appears anywhere it is enabled, including newtab and dictionary source lists, without requiring API keys.
- JPDB and Jiten source availability follows source settings, not unrelated API-key status.
- Userscript and docs/newtab app agree on settings and local dictionary store access where technically possible.
- Dictionary install progresses, handles download errors, and completes for Jitendex.
- Imported Yomitan dictionaries appear in search results and popovers on hosted docs/newtab.

## P0-06 Dictionary Content Quality, KANJIDIC Scope, Images, And Monolinguals

Source evidence:
- Kanji dictionaries such as KANJIDIC should be limited to kanji pages in popovers rather than definition pages.
- Add monolingual dictionaries to recommended downloads with homepage info.
- Test monolingual lookups and confirm everything there is parsed.
- Jitendex dictionary images show a strange empty box instead of an image.

Acceptance:
- Kanji-only dictionaries show only on kanji-oriented pages/sections.
- Recommended downloads include appropriate monolingual dictionaries with homepage/attribution info.
- Monolingual lookup content renders and is parsed with the reader scanner without nested popovers.
- Dictionary images load when allowed and degrade gracefully with alt/fallback if blocked.
- Add fixtures/tests for image entries and monolingual entries.

## P0-07 Settings Simplification, Sync, Localization, And Layout Audit

Source evidence:
- Settings are not synced across tabs between `newtab/index.html?q=%E5%A5%BD` and `/yomu-reader/`.
- Japanese settings layout fields are misaligned versus English.
- Appearance preview is not centered.
- Remove warning about Safari evicting data because GM store is used.
- Remove settings that do not make sense: `jpdbDefinitionsEnabled`, `localDictionariesEnabled`, `dictionarySourcesInitiallyExpanded`, and `localDictionaryMaxResults`.
- Respect all furigana settings, remove `automatic`, and show the actual selected option.
- Test settings spacing/alignment on mobile, tablet, desktop, English, Japanese, and Japanese with ruby.

Acceptance:
- Settings changes propagate across tabs/pages and persist through userscript/docs/newtab contexts.
- Removed settings no longer appear and their stale state no longer gates dictionary results.
- Furi mode UI has no vague `automatic` option and reflects the real selected behavior.
- Japanese labels with ruby align cleanly in settings grids.
- Appearance preview is centered.
- Mobile/tablet/desktop visual checks pass for English and Japanese.

## P0-08 Furigana, Pitch Underlines, Baselines, And Full-Page Enhancement Coverage

Source evidence:
- Underlines are added at different line heights in screenshots.
- Very little text on newtab `?q=よむ` gets furigana and pitch enhancements.
- Chrome and text on `newtab/index.html` are missing ruby and pitch.
- Next-step cards are missing pitch accent underlines.
- Install step links show partial pitch/ruby behavior.
- Default mode only shows difficult kanji, but line spacing is still huge when no furigana appears.

Acceptance:
- Ruby and pitch colorization cover stable Japanese UI text across hosted docs and newtab.
- Baseline/underline positioning is consistent across inline text, cards, buttons, headings, and settings controls.
- Line spacing does not inflate when ruby is suppressed by settings or difficulty mode.
- Furigana settings are respected exactly.
- Add smoke tests for underline baseline and coverage on docs/newtab pages.

## P0-09 Docs Routing, Landing Hero, And Hosted Page Polish

Source evidence:
- `https://hrussellzfac023.github.io/getting-started` is a 404.
- Main hero spacing needs to be fixed and made visually strong.
- `https://hrussellzfac023.github.io/yomu-reader/newtab/index.html` is not localized.
- The hosted docs/newtab should feel complete in Japanese mode.

Acceptance:
- Broken `/getting-started` link is fixed to the correct hosted path.
- Hero spacing works across desktop/tablet/mobile and leaves adjacent content readable.
- Newtab root page localizes visible chrome in Japanese.
- No untranslated visible strings in the affected docs/newtab flows.
- Browser screenshots or DOM checks cover desktop and mobile.

## P0-10 Newtab Study Mode, Keyless Study, Anki, And Deck Selection

Source evidence:
- Kanji tab gets stuck on `さらに漢字を探しています...`.
- Word tab gets stuck on `さらに単語を探しています...`.
- Study should work for kanji and words without keys, Anki, or dictionaries.
- Anki is connected but no Anki cards appear on the study tab.
- Verify Anki cards working in Playwright on newtab and in popover.
- Make Anki deck selection simpler, like JPDB deck dropdown.
- Support deck selection for Jiten too.

Acceptance:
- Study mode has keyless fallback material for words and kanji.
- Word and kanji tabs do not get stuck in indefinite loading states.
- Connected Anki cards appear and can be reviewed/mined.
- Newtab and popover Anki actions are verified with Playwright or existing Anki smoke scripts.
- Deck/source selector UX is consistent for JPDB, Anki, and Jiten.

## P0-11 Newtab Kanji Sections And Search Result UI Structure

Source evidence:
- Kanji buttons are bigger than needed.
- Kanji sections have different heights from other sections and section boundaries are unclear, especially for words with many kanji.
- Images 7, 8, and 9 show repeated, uneven kanji/detail sections and unclear separation.

Acceptance:
- Kanji buttons are compact and touch-friendly without dominating result cards.
- Kanji, readings/components, RTK, component graph, Uchisen, and Immersion Kit sections have clear headings and consistent collapsed/expanded rhythm.
- Multiple-kanji words remain readable and scannable.
- Layout verified on desktop and mobile.

## P0-12 Selection Popover Actions And Large Text Handling

Source evidence:
- Selection popup is not showing for large sentences.
- Selection popover should contain pills such as send to study, copy, JPDB, etc. according to user settings.
- Image 11 shows the selection popup rendering a basic parse list without expected actions.

Acceptance:
- Large Japanese selections open a responsive popover.
- Selection popover includes configured action pills and source actions.
- Parsing remains sentence-aware and not just isolated tile output.
- Popover layout is usable on mobile and desktop.

## P0-13 Autonomous User-Journey Papercut Audit

Source evidence:
- User requested a separate autonomous thread to try journeys like a user and search for similar papercuts.

Acceptance:
- Exercise install, settings, newtab search, dictionary install, popover lookup, JPDB/Jiten/local lookup, study, Anki, Japanese localization, and mobile/tablet flows.
- File any newly discovered P0/P1 issues in the backlog or fix small papercuts directly if safe.
- Add or update smoke scripts that would catch the discovered issues.

Audit findings filed by P0-13:
- P1: `npm run smoke:anki` timed out waiting for `.jpdb-reader-popover .jpdb-reader-anki-existing` in `scripts/anki-mining-smoke.mjs`, so the popover existing-card Anki status/action path needs follow-up coverage and a fix.
- P1: `npm run smoke:mobile-docs` reached the mobile docs/settings portion and timed out waiting for `.jpdb-reader-settings`; the harness rect bug was fixed, but the mobile settings opening path still needs follow-up verification/fix.
- Push a release if any user-facing fixes land.

## P0-14 YouTube Signed-In Feed/Watch Enhancement Coverage And Performance

Source evidence:
- User reports pitch, underline, and color information is not showing on signed-in `https://www.youtube.com/`.
- User reports it is also not showing on video/watch pages.
- Images: `/var/folders/pw/w51pz2xd16z4x521kb7m7x3r0000gq/T/codex-clipboard-c9b830a4-2bc0-4575-adfa-07fe3fe99784.png` and `/var/folders/pw/w51pz2xd16z4x521kb7m7x3r0000gq/T/codex-clipboard-69ec957b-366a-4f2d-9f35-d5f44f367c03.png`.
- User requires this to feel instant and not damage YouTube performance.
- User reports YouTube content shift regressions while scrolling and frequent black screens.
- Image: `/var/folders/pw/w51pz2xd16z4x521kb7m7x3r0000gq/T/codex-clipboard-a37890fd-5883-408f-91b6-f3c2c326c53f.png`.
- User reports OCR is being triggered on YouTube thumbnails. This must not happen.
- Image: `/var/folders/pw/w51pz2xd16z4x521kb7m7x3r0000gq/T/codex-clipboard-4a003200-9dce-43a5-bc18-a9bda1477f80.png`.

Acceptance:
- Find the root cause for missing pitch/underline/color on signed-in YouTube home/feed and watch pages.
- Restore colorization/pitch underline/ruby behavior for Japanese text in feed titles, metadata, chips where appropriate, watch page text, captions/subtitles, and transcript/sidebar text.
- Keep performance responsive on virtualized YouTube surfaces; avoid rescanning huge DOM regions synchronously.
- Fix the scroll/content-shift/black-screen regression and identify whether scanner DOM mutation, virtualized grid interaction, CSS, or observer scheduling is responsible.
- Do not trigger OCR on YouTube thumbnails or thumbnail images. OCR should remain available only for intentional user actions/surfaces where it makes sense, not automated feed scanning.
- Preserve existing protections against YouTube title/ruby layout bugs.
- Add or update YouTube smoke/performance scripts and capture browser evidence in signed-in context if available.
- Push a release after the fix lands.

## P0-15 YouTube JPDB Channel Shelf Subscription Filtering

Source evidence:
- User reports `jpdb-youtube-channel-shelf-list` is showing channels they are already subscribed to.
- Expected behavior: if every candidate channel is already subscribed, the shelf should show none.

Acceptance:
- Find the data source and filtering path for `jpdb-youtube-channel-shelf-list`.
- Exclude already-subscribed channels reliably in signed-in YouTube context.
- If all channels are subscribed, hide the shelf or show an empty state that does not recommend anything.
- Add focused tests or a browser smoke for subscribed versus unsubscribed channel candidates.
- Preserve performance on YouTube feed/watch pages.
- Push a release after the fix lands.

## P0-16 YouTube Console Errors And Broken Action Pills

Source evidence:
- On YouTube, clicking action pills such as open in Jiten, open in JPDB, and open in Yomu does nothing.
- Console errors observed on YouTube include:
  - `Error: Not allowed to define cross-origin object as property on [Object] or [Array] XrayWrapper`
  - CSP blocks for inline scripts under `script-src` with nonce/hash requirements.
  - `Cross-Origin Request Blocked: The Same Origin Policy disallows reading the remote resource at data:text/plain;base64,Cg==.`
  - YouTube preload warning for `https://i.ytimg.com/generate_204`.
- Additional console evidence reported 2026-06-15:
  - CSP event-handler block for `script-src-attr`, source `_rtf(this) search`.
  - Repeated `XrayWrapper` errors at `content-script.js:355:764`.
  - `Empty string passed to getElementById()` from YouTube/Google scripts.
  - CORS blocks for `https://ep1.adtrafficquality.google/getconfig/sodar...` and `https://play.google.com/log?...`.
  - `Layout was forced before the page was fully loaded` warning from YouTube `app`.
  - Repeated inline-script CSP blocks from `page.js line 20 > eval:75072:14`.
- Follow-up evidence added during execution:
  - The `XrayWrapper` error repeats from `content-script.js:355`.
  - CSP inline script errors may be YouTube/page noise, but must be confirmed as not caused by Yomu injection.
  - The `data:text/plain;base64,Cg==` CORS block is likely a Yomu audio/data-url fetch path treating a `data:` URL like HTTP and should be fixed if confirmed.

Acceptance:
- Find why YouTube action pills do not fire or cannot open their targets.
- Jiten, JPDB, Yomu, copy, and configured action pills work from YouTube popovers/surfaces without breaking native YouTube clicks.
- Avoid inline script/eval paths that violate YouTube CSP. Do not rely on unsafe page injection for pill actions.
- Avoid cross-origin object writes that trigger Firefox/Tampermonkey XrayWrapper errors.
- Classify console noise: fix Yomu-caused errors; document browser/YouTube-only warnings if not caused by Yomu.
- Add a YouTube-focused browser smoke or DOM test proving pill actions dispatch/open expected URLs.
- Preserve YouTube performance and coordinate with P0-14 if shared scanner/action plumbing is touched.
- Rebase onto current `origin/main`, integrate into `main`, push, and publish/trigger a release. Verify with `gh release view <tag>`.

## P0-17 Live Regression Revalidation And Reopen Gate

Source evidence:
- User reports that many original issues remain even after the worker changes and releases through at least `v0.7.27`.
- Commit history shows many P0 threads landed and released, but remaining bugs must be verified against the live hosted site/userscript rather than inferred from commits.

Acceptance:
- Build a verification matrix for every P0 item against current `origin/main` and the latest hosted/released artifact.
- Reproduce the original flows, especially:
  - hosted newtab search for `よむ`, `読み取る`, and `好`;
  - JPDB/Jiten/local dictionary visibility and action pills;
  - Immersion Kit card styling/audio/next/previous;
  - settings sync/layout in English and Japanese with ruby;
  - docs hero/localization and `/getting-started` routing;
  - selection popup for long Japanese text and configured action pills;
  - study/Anki/deck flows;
  - YouTube signed-in feed/watch text enhancement, no thumbnail OCR, no black screen/content shift, channel shelf filtering, and action pills.
- For each item, mark one of: `verified fixed`, `still reproduces`, `partially fixed`, `blocked by missing credentials/context`, or `needs user confirmation`.
- If an issue still reproduces, create a concrete follow-up in this backlog with exact URL, settings, expected behavior, actual behavior, screenshot/log evidence where possible, and the responsible subsystem/thread.
- Fix small verified regressions directly when safe, but do not mask larger failures with superficial UI patches.
- Route unresolved concrete failures back to the relevant worker thread, or spawn a narrowly scoped fix worker if the original thread is inactive.
- Push any backlog updates and user-facing fixes to `main`, then publish/trigger a release for fixes and verify the release.

Live revalidation matrix added 2026-06-15 from worker `p0-17-live-regression-revalidation`.
Updated after fresh user reports on 2026-06-15: fresh screenshots/live reports are authoritative over commit/test/release inference. A row must not be upgraded to `verified fixed` without browser/live verification of the exact reported journey after the relevant release/deploy.

| Item | Current `origin/main` status | Latest hosted/released status | Evidence / routing |
| --- | --- | --- | --- |
| P0-01 Active/passive scanning and popover parsing | `partially fixed` | `still reproduces` | `npm run smoke:keyless-popover` passed a narrow fixture, but fresh P0-25/P0-28 reports show real YouTube controls and dictionary action pills are still not behaving like enhanced passive/native controls. Route active/passive/action-swallowing failures to P0-01/P0-25/P0-28. |
| P0-02 Parser correctness and sentence context | `partially fixed` | `needs user confirmation` | `npm run test:ci` includes `sentence-context.test.ts` and parser fallback tests, but large live selection sentence parsing was not fully reproduced in a browser. Route large-selection parsing failures to P0-02/P0-12. |
| P0-03 Immersion Kit rendering and controls | `partially fixed` | `still reproduces` | `npm run smoke:audio-newtab` and `npm run smoke:audio-popover` passed for fixture audio/control plumbing, but P0-19 reports multi-second hosted Immersion Kit example/image/audio latency and P0-27 reports the Immersion Kit source label stretched/split. Route to P0-19/P0-27/P0-03. |
| P0-04 Newtab search, query params, result actions | `partially fixed` | `still reproduces` | Hosted searches rendered results in the earlier probe, but P0-28 now reports dictionary popover action pills do not navigate. Anki pill visibility remains settings/context-dependent. Route action regressions to P0-04/P0-16/P0-28. |
| P0-05 Jiten, JPDB, local dictionaries, store coherence | `partially fixed` | `still reproduces` | `npm run smoke:jiten-newtab` passed mocked Jiten/JPDB study paths; live Jiten check skipped without `YOMU_JITEN_API_KEY`. P0-21 now reports the Jiten pill is present but no Jiten source panel appears in expanded dictionary sources. Route to P0-21. |
| P0-06 Dictionary content quality, KANJIDIC, images, monolinguals | `partially fixed` | `needs user confirmation` | `npm run test:ci` covered dictionary images/source-state tests. Live monolingual and KANJIDIC scope flows were not revalidated with installed user dictionaries. Route concrete dictionary rendering failures to P0-06. |
| P0-07 Settings simplification, sync, localization, layout | `partially fixed` | `still reproduces` | `npm run smoke:mobile-docs` passed mobile settings puck/opening and Japanese mobile newtab layout, but P0-22 reports layout still bad and P0-23 reports hosted settings are not self-enhanced without the userscript. Route to P0-22/P0-23. |
| P0-08 Furigana, pitch underlines, full-page coverage | `partially fixed` | `still reproduces` | `npm run smoke:enhancement-coverage` passed fixture docs/newtab coverage, but hosted/fresh evidence shows `学習能力` pitch/color missing (P0-20), YouTube chrome missing ruby/furigana (P0-25), and the enrichment fetch pipeline is too serial (P0-26). Route to P0-20/P0-25/P0-26. |
| P0-09 Docs routing, hero, hosted polish | `still reproduces` | `still reproduces` | `curl -I https://hrussellzfac023.github.io/getting-started` returned `404`; `https://hrussellzfac023.github.io/yomu-reader/getting-started` returned `200`. Route routing/hero/localization work to P0-09. |
| P0-10 Study mode, Anki, deck selection | `partially fixed` | `still reproduces` | Older smokes passed keyless/Jiten/JPDB/Anki harnesses, but P0-29 fresh hosted evidence shows the Word study tab stuck on `Looking for more words...` at `https://hrussellzfac023.github.io/yomu-reader/newtab/index.html?q=%E8%AA%AD%E3%81%BF%E5%8F%96%E3%82%8B`. Route to P0-29/P0-10; do not mark fixed from smokes alone. |
| P0-11 Kanji sections and search result UI structure | `partially fixed` | `partially fixed` | Hosted searches rendered kanji sections, but `学習能力` and other multi-kanji cards still showed dense/repetitive text such as `学学習能力...`; no full visual pass was performed. Route remaining visual structure issues to P0-11. |
| P0-12 Selection popover actions and large text handling | `partially fixed` | `needs user confirmation` | `npm run smoke:keyless-popover` passed a click popover with action pills. The original long Japanese selection flow was not fully reproduced in hosted/browser context. Route long-selection failures to P0-12/P0-02. |
| P0-13 Autonomous journey papercut audit | `partially fixed` | `partially fixed` | Several smoke scripts cover narrow harness papercuts, but fresh concrete follow-ups now run through P0-29. Keep P0-13 open for broad user-journey discovery and live journey verification. |
| P0-14 YouTube signed-in feed/watch coverage and performance | `partially fixed` | `still reproduces` | Fixture/proof YouTube scripts passed, but fresh signed-in/live evidence still shows feed/watch pitch missing (P0-24), chrome/control text missing ruby/furigana (P0-25), and likely serial enrichment delay (P0-26). Route to P0-14/P0-24/P0-25/P0-26. |
| P0-15 YouTube channel shelf subscription filtering | `blocked by missing credentials/context` | `blocked by missing credentials/context` | Fixture YouTube smoke exercised channel shelf rendering but not a signed-in subscription graph. Requires signed-in YouTube account context or a targeted subscription fixture. Route to P0-15. |
| P0-16 YouTube console errors and action pills | `partially fixed` | `still reproduces` | Fixture/popover flows showed pills in a harness, but P0-28 now reports dictionary popover action pills do not navigate, and signed-in real YouTube pill behavior remains unverified. Route to P0-16/P0-28. |
| P0-17 Live revalidation gate | `still reproduces` | `still reproduces` | Matrix updated after fresh reports. Latest hosted Pages probe serves `0.7.36-de1616fb9472`; GitHub Releases through `v0.7.35` are non-draft and include `yomu.user.js`, while `v0.7.36` is currently tag/workflow-only at the time of this probe. Fresh P0-25 through P0-29 reports show the previous matrix was too optimistic; unresolved journeys remain routed below and still require exact post-release live/browser evidence. |
| P0-18 Duplicate reading text with furigana | `partially fixed` | `partially fixed` | Live check on `https://hrussellzfac023.github.io/yomu-reader/newtab/index.html?q=%E5%AD%A6%E7%BF%92%E8%83%BD%E5%8A%9B` after the `v0.7.31`/hosted `0.7.32+` deploy confirmed the `Words` result has ruby `がくしゅうのうりょく`, meta `#32900`, and no duplicate plain reading outside `<ruby>`. Hosted `0.7.36-de1616fb9472` also shows the result token with concrete `jpdb-pitch-nakadaka`; do not close globally because Kanji-card reading repetition remains a separate surface. Route remaining duplication/reading display issues to P0-11/P0-08. |
| P0-19 Immersion Kit performance | `still reproduces` | `still reproduces` | Fresh user report: changing Immersion Kit image/example or pressing audio play takes several seconds on hosted `学習能力`; P0-27 also reports Immersion Kit source-label layout breakage. Route to P0-19/P0-27. |
| P0-20 Newtab pitch coverage | `partially fixed` | `still reproduces` | Hosted `0.7.36-de1616fb9472` preserves the `学習能力` result-area improvement: word result has ruby `がくしゅうのうりょく`, concrete `jpdb-pitch-nakadaka`, and kanji/result cards have pitch classes. Still open: topbar/chrome action safety is broken; clicking the parsed overflow `設定` control searched/navigated to `?q=統計` instead of opening settings (`settingsCount: 0`). Parsed chrome must preserve native/control actions. Route to P0-20/P0-23/P0-08/P0-26. |
| P0-21 Jiten source missing | `partially fixed` | `needs release verification` | Hosted `0.7.36-de1616fb9472` expanded `学習能力` result now shows a `Jiten` source panel after `JPDB` with empty-state text `Jiten定義なし。`, in addition to the Jiten action pill. `v0.7.36` release workflow is still in progress at this probe, so keep open until the release object/assets exist and verify `読み取る`/`よむ` too. Route to P0-21/P0-05. |
| P0-22 Settings layout still bad | `still reproduces` | `still reproduces` | New user screenshots show bad settings layout remains. Route to P0-22/P0-07. |
| P0-23 Hosted settings not self-enhanced | `still reproduces` | `still reproduces` | Fresh screenshots show hosted settings are not self-enhanced without userscript injection. Hosted `0.7.36-de1616fb9472` check in Japanese mode still cannot reach the dialog because clicking the visible parsed `設定` overflow button searches `統計` instead of opening settings (`settingsCount: 0`); English-mode settings only enhanced preview sample text while tabs/buttons remained plain. Route to P0-23/P0-20/P0-07/P0-08/P0-26. |
| P0-24 YouTube feed/watch pitch missing | `still reproduces` | `still reproduces` | Fresh signed-in YouTube feed/watch screenshots show pitch still missing. Route to P0-24/P0-14/P0-25/P0-26. |
| P0-25 YouTube chrome/control text missing ruby/furigana | `partially fixed` | `needs live verification` | Source fix for YouTube chrome scanning landed on `origin/main` in `v0.7.32`, and GitHub Release `v0.7.32` includes `yomu.user.js`. Hosted Pages is now `0.7.36`, but the signed-in YouTube journey still needs post-release verification for chips/sidebar/topbar `作成` without OCR/content-shift regressions. Keep routed to P0-25 with P0-01/P0-14/P0-16 coordination. |
| P0-26 Furigana/pitch enrichment fetch pipeline too serial | `still reproduces` | `still reproduces` | Fresh user report says enrichment/furigana fetches are too serial and should use bounded concurrent batches. Treat as shared infrastructure for P0-08/P0-14/P0-19/P0-20/P0-23/P0-24/P0-25. |
| P0-27 Immersion Kit source label stretched/split | `still reproduces` | `still reproduces` | Fresh screenshot shows the Immersion Kit source label split/stretched as `イ   マージ   ョ   ン   キット` in a popover/source accordion. Route to P0-27/P0-03/P0-08. |
| P0-28 Dictionary popover action pills do not navigate | `partially fixed` | `partially fixed` | Source fix for inert hover-popover action pills landed on `origin/main` in `v0.7.33`; GitHub Release `v0.7.33` is non-draft and includes `yomu.user.js`. Worker smokes clicked Jiten/JPDB/Jisho/Yomu/Copy in normal and YouTube fixture popovers. Post-release hosted check on `学習能力` expanded the word result and clicking the `Jiten` pill navigated to `https://jiten.moe/parse?text=%E5%AD%A6%E7%BF%92%E8%83%BD%E5%8A%9B`; the exact reported YouTube/hover popover journey still needs live verification before closure. Route remaining failures to P0-28/P0-04/P0-16/P0-01. |
| P0-29 Newtab Word study stuck looking for more words | `still reproduces` | `still reproduces` | Fresh hosted report at `https://hrussellzfac023.github.io/yomu-reader/newtab/index.html?q=%E8%AA%AD%E3%81%BF%E5%8F%96%E3%82%8B` shows Word study stuck on `Looking for more words...`. Route to P0-29/P0-10. |

Verification commands run:
Fresh update note: the commands below are historical evidence only. Under the updated Common Worker Contract, they are not sufficient to mark a P0 fixed without exact live/browser verification of the reported journey after release.
- `npm ci`
- `npm run build`
- `npm run smoke:jiten-newtab`
- `npm run smoke:study-personas`
- `npm run smoke:enhancement-coverage`
- `npm run smoke:audio-newtab`
- `npm run smoke:keyless-popover`
- `npm run smoke:audio-popover`
- `npm run smoke:mobile-docs`
- `npm run smoke:youtube`
- `npm run smoke:youtube-grid`
- `npm run smoke:youtube-ruby-proof`
- `npm run smoke:youtube-shorts-skip`
- `npm run smoke:anki`
- `YOMU_LIVE_EXPECT_PACKAGE_VERSION=1 npm run smoke:live-browser`
- `npm run test:ci` started all shards; shards 1, 2, and 4 passed, while shard 3 reported its individual tests passed but hit the 9-minute wrapper timeout (`exit 124`) after a very slow `settings-dialog-controller.test.ts` run.

## P0-18 Duplicate Reading Text When Furigana Is Visible

Source evidence:
- User screenshot: `/var/folders/pw/w51pz2xd16z4x521kb7m7x3r0000gq/T/codex-clipboard-7e5ccf48-a623-4afc-a361-312f6c92850c.png`.
- Search/result header for `学習能力` shows furigana `がくしゅうのうりょく` above the kanji and also repeats `がくしゅうのうりょく` inline beside the headword before `#32900`.
- User expectation: when furigana is visible, do not also show duplicate hiragana/reading text.

Acceptance:
- Result headers/headwords do not show the same reading twice.
- If ruby/furigana is enabled for a headword, suppress or visually de-emphasize the adjacent plain reading when it is identical to the ruby reading.
- Preserve useful alternate readings: do not remove non-duplicate readings, disambiguating readings, or reading metadata that is not already represented by the ruby.
- Apply consistently across newtab search results, popovers, dictionary result headers, and study cards where the same duplication can occur.
- Add a regression test or DOM smoke for `学習能力` or a similar kanji headword with visible furigana.
- Coordinate with P0-08 because this touches furigana/pitch display and line-height behavior.
- Push to `main`, publish/trigger a release, and verify the hosted/released result no longer duplicates the reading.

## P0-19 Immersion Kit Interaction Performance On Newtab

Source evidence:
- User reports major performance issues on `https://hrussellzfac023.github.io/yomu-reader/newtab/index.html?q=%E5%AD%A6%E7%BF%92%E8%83%BD%E5%8A%9B`.
- The slow path is around Immersion Kit: changing the image/example or pressing play on audio takes several seconds.
- This remains after the Immersion Kit controls release, so the interaction must be profiled on current `main`/hosted assets rather than assumed fixed by P0-03.

Acceptance:
- Reproduce and measure the delay for Immersion Kit next/previous image changes and audio play on the `学習能力` newtab URL.
- Identify the root cause before patching: image decode/network, audio URL probing/fallback, full card re-render, scanner/ruby reparsing, synchronous layout, storage access, or unnecessary source reload.
- UI feedback for next/previous and play must be immediate. Media may still stream over network, but controls should not appear frozen for seconds.
- Cache or prefetch only where it is clearly bounded and does not bloat memory or block initial page load.
- Avoid reparsing/recolorizing the entire result page when only the active Immersion Kit example changes.
- Add focused tests or a Playwright/browser smoke that measures interaction latency or at least asserts no full source reload/reparse is triggered by next/play.
- Coordinate with P0-03 and P0-17 because this is a reopened Immersion Kit regression.
- Push to `main`, publish/trigger a release, and verify the hosted/released `学習能力` page interaction is responsive.

## P0-20 Newtab Search Pitch Accent Coverage Missing

Source evidence:
- User screenshot: `/var/folders/pw/w51pz2xd16z4x521kb7m7x3r0000gq/T/codex-clipboard-efe94250-3a90-4d57-947e-66c4f38bc056.png`.
- On the newtab search page for `学習能力`, pitch accent/colorization is missing from every visible Japanese text surface: page title/search heading, search input value, primary result card, kanji cards, and the word result.
- The same screenshot also shows duplicate reading text, covered separately by P0-18.
- Attached DOM evidence: `/Users/heru/.codex/attachments/825e429b-582a-44ea-961f-8574467c0260/pasted-text.txt` shows the word result token rendered as `jpdb-pitch-unknown` for `学習能力`, while page chrome/search suggestions/kanji cards are not wrapped with reader pitch/color tokens.
- Live hosted `0.7.36-de1616fb9472` evidence improves only part of this: `学習能力` word result renders with ruby `がくしゅうのうりょく` and concrete `jpdb-pitch-nakadaka`, but page chrome/settings actions still fail. In Japanese mode, opening the overflow menu and clicking the visible parsed `設定` button navigated/searched to `?q=統計` and no settings dialog appeared (`settingsCount: 0`).

Acceptance:
- Reproduce on `https://hrussellzfac023.github.io/yomu-reader/newtab/index.html?q=%E5%AD%A6%E7%BF%92%E8%83%BD%E5%8A%9B` and/or a local built equivalent.
- Confirm the active pitch/color/furigana settings being used; do not hide behind a stale disabled setting unless the UI clearly reflects that state.
- Newtab search result surfaces for `学習能力` show pitch accent classes/underlines and colorization wherever pitch data is available.
- If pitch data is missing, identify whether the failure is parser lookup, source ranking, result rendering, CSS, or a settings-store mismatch.
- Apply consistently across page chrome/search text, result headers, kanji cards, word cards, popovers, and study cards that reuse the same result renderer.
- Parsed chrome/control text must preserve the original click behavior; settings/menu buttons must not become word-search triggers.
- Preserve performance: adding pitch coverage must not trigger whole-page reparsing loops or slow Immersion Kit interactions.
- Add regression tests or DOM smoke that asserts pitch classes/underlines are present for `学習能力` search results when pitch display is enabled.
- Coordinate with P0-08 and P0-18 because they touch shared furigana/pitch/headword display paths.
- Push to `main`, publish/trigger a release, and verify the hosted/released page shows pitch accents.

## P0-21 Jiten Source Missing From Expanded Newtab Dictionary Results

Source evidence:
- User screenshot: `/var/folders/pw/w51pz2xd16z4x521kb7m7x3r0000gq/T/codex-clipboard-9c09b9cf-5070-407f-ab18-0787212bb936.png`.
- User explicitly reports: "I dont see Jiten dictionary in the list here!!!"
- The expanded `学習能力` word result shows action pills for `Jiten`, `JPDB`, `コピー`, and `Yomu`, but the dictionary source list below contains `JPDB`, `イマージョンキット`, and `漢字`; there is no `Jiten` source panel.
- Attached DOM evidence: `/Users/heru/.codex/attachments/825e429b-582a-44ea-961f-8574467c0260/pasted-text.txt` shows the search result/detail structure for this page and confirms the missing source in the visible source list.

Acceptance:
- Reproduce on `https://hrussellzfac023.github.io/yomu-reader/newtab/index.html?q=%E5%AD%A6%E7%BF%92%E8%83%BD%E5%8A%9B`.
- When Jiten is enabled, Jiten appears as a dictionary source panel in expanded newtab word results, not only as an external action pill.
- Jiten source availability must not require JPDB API keys or Anki/local dictionary state.
- If Jiten has no result, show a clear empty/error state in the Jiten source panel rather than omitting the source silently.
- Verify the same behavior for `読み取る`, `よむ`, and `学習能力`.
- Add tests or a DOM smoke for Jiten source visibility in newtab expanded word results.
- Coordinate with P0-05 because this is a reopened dictionary-source/store issue.
- Push to `main`, publish/trigger a release, and verify the hosted/released page shows Jiten in the source list.

## P0-22 Settings Layout Still Broken In Japanese/Ruby Mode

Source evidence:
- User screenshot: `/var/folders/pw/w51pz2xd16z4x521kb7m7x3r0000gq/T/codex-clipboard-9533b418-c312-473a-9393-e4ed9dad01ce.png`.
- User screenshot: `/var/folders/pw/w51pz2xd16z4x521kb7m7x3r0000gq/T/codex-clipboard-df8d976e-6a0e-48d6-8db4-24c8b0cde6c1.png`.
- User reports settings layout is still bad after previous settings fixes.
- Current Japanese/ruby settings view has large uneven whitespace, controls scattered across very wide rows, awkward wrapping/alignment, and field groups that are hard to scan.

Acceptance:
- Reproduce settings layout in Japanese with ruby enabled on desktop, tablet, and mobile.
- Settings sections should use a consistent, compact, scannable grid: related controls align in rows/columns, checkbox labels align with controls, select/input fields align with labels and helper text, and no section has huge empty gaps.
- Media/audio settings in particular must be audited: example provider/source/sort controls, checkboxes, numeric inputs, and audio playback options should align cleanly.
- Keep English layout and no-ruby Japanese layout professional too; do not fix ruby mode by breaking other modes.
- Text must not overflow, overlap, or create unnecessary line-height expansion.
- Add a visual/DOM smoke or responsive layout check for Japanese settings with ruby at desktop/tablet/mobile sizes.
- Coordinate with P0-07 because this is a reopened settings layout issue.
- Push to `main`, publish/trigger a release, and verify the hosted/released settings page layout is fixed.

## P0-23 Hosted Newtab Settings Text Not Enhanced Without Userscript

Source evidence:
- User screenshot: `/var/folders/pw/w51pz2xd16z4x521kb7m7x3r0000gq/T/codex-clipboard-415f697d-1087-4e69-bf13-2dd4a285302b.png`.
- User screenshot: `/var/folders/pw/w51pz2xd16z4x521kb7m7x3r0000gq/T/codex-clipboard-df802ee9-ac94-4abc-b7ba-b3a8c445260d.png`.
- URL: `https://hrussellzfac023.github.io/yomu-reader/newtab/index.html?q=%E5%AD%A6%E7%BF%92%E8%83%BD%E5%8A%9B`.
- User notes this is the settings page where the userscript is not enabled, and settings text is not parsed with pitch/ruby as expected.
- Visible Japanese settings labels such as `よむ 設定`, tab labels, `設定を検索`, Anki labels, field labels, helper text, and action buttons appear as plain text rather than enhanced reader text.
- Follow-up screenshot on the search page/settings modal shows `よむ 設定`, tabs (`外観`, `API`, `ソース`, `メディア`, `採掘`, `学習`, `ショートカット`, `ヘルプ`), appearance labels, select values, helper text, color labels, `キャンセル`, and `保存` still without furigana or pitch.
- Live hosted `0.7.36-de1616fb9472` evidence: English-mode settings opened but only preview sample text had reader spans; tab/action labels remained plain. In Japanese mode, clicking the visible parsed `設定` overflow button did not open settings and instead searched `統計`; the exact Japanese settings enhancement journey remains unverified and still failing.

Acceptance:
- Hosted newtab/settings app enhances its own Japanese UI text with ruby/pitch/colorization even when the browser userscript is not active on the page.
- Enhancement should use the app/runtime scanner safely, not require a separate userscript injection or duplicate global bootstrap.
- Settings controls remain native/passive: clicking tabs, selects, inputs, checkboxes, links, Cancel, and Save works normally while hover lookup works where appropriate.
- Enhancing settings/menu labels must not break their control actions; the `設定` overflow button must open settings, not initiate a word search.
- Avoid double-enhancement when the userscript is active on the hosted page.
- Apply to all settings panels, including Anki, Media, Source, Learning, Shortcuts, Help, and the settings search field/results.
- Respect the user's furigana/pitch settings and do not inflate line-height when furigana is suppressed.
- Add a DOM/browser smoke for hosted newtab settings with userscript disabled, Japanese mode, and ruby/pitch enabled.
- Coordinate with P0-01, P0-07, P0-08, and P0-22 because this touches passive controls, settings layout, and full-page enhancement coverage.
- Push to `main`, publish/trigger a release, and verify the hosted/released settings page parses correctly without the userscript.

## P0-24 YouTube Feed/Watch Pitch Accent Coverage Still Missing

Source evidence:
- User screenshot: `/var/folders/pw/w51pz2xd16z4x521kb7m7x3r0000gq/T/codex-clipboard-c82afc92-e226-428e-b4dc-6cee2bcba3dc.png`.
- User screenshot: `/var/folders/pw/w51pz2xd16z4x521kb7m7x3r0000gq/T/codex-clipboard-c2d00de9-70f5-45a5-a7d5-bb48ed9f651d.png`.
- User reports none of the visible YouTube elements are getting pitch accent as expected.
- Visible signed-in YouTube home/feed Japanese text includes titles, channel names/metadata, chips, and subtitles/thumbnail text areas, but no reader pitch/color/ruby enhancement is visible.
- Visible signed-in YouTube watch-page Japanese text includes the video title, description, subtitle/caption overlay, and transcript/sidebar rows, but no reader pitch/color/ruby enhancement is visible.
- This is a still-reproducing form of P0-14 after prior YouTube enhancement releases.

Acceptance:
- Reproduce in signed-in YouTube home/feed and watch-page contexts.
- Japanese text in feed titles, metadata, chips, video title, description, transcript/sidebar rows, and other stable text nodes receives pitch/color/ruby enhancement where data is available.
- Caption/subtitle overlay handling must be correct: enhance real overlay text only if it can be done without disrupting video playback, timing, or native caption styling; otherwise clearly document the safer boundary and ensure transcript/sidebar text is enhanced.
- Do not OCR thumbnails automatically. Thumbnail image text should remain untouched unless an intentional OCR action is invoked.
- Preserve YouTube performance: no black screens, content shift, or synchronous full-grid rescans while scrolling.
- Respect active/passive semantics for YouTube links/buttons so native navigation/clicks still work.
- Add/update YouTube smoke/performance tests that prove visible feed/watch text gets reader classes and thumbnails are excluded from OCR.
- Coordinate with P0-14 and P0-16 because this touches YouTube scanner/action plumbing.
- Push to `main`, publish/trigger a release, and verify the hosted/released userscript enhances YouTube feed text.

## P0-25 YouTube Chrome/Control Text Missing Ruby And Furigana

Source evidence:
- User reports YouTube chrome-like elements are missing ruby/furigana, separate from feed/watch content titles.
- Attachment: `/Users/heru/.codex/attachments/49bfef64-8962-4d36-a24d-7a2da1715f18/pasted-text.txt`.
- Attachment: `/Users/heru/.codex/attachments/af2736db-eeb1-4f7a-9033-10a47611654b/pasted-text.txt`.
- YouTube filter chip DOM contains interactive chip buttons with Japanese text such as `すべて`, `ポッドキャスト`, `ゲーム`, `ニュース`, `音楽`, `ミックス`, `ライブ`, `観光地`, `最近アップロードされた動画`, `視聴済み`, and `新しい動画の発見`.
- YouTube mini-guide DOM contains navigation links and duplicated tooltip/hidden button labels for `ホーム`, `ショート`, `登録チャンネル`, and `マイページ`.
- User-provided topbar button example: a `yt-button-shape` create button with `aria-label="作成"` and visible text `<span class="ytAttributedStringHost ...">作成</span>`.

Acceptance:
- YouTube chrome/control text that is stable and visible gets reader enhancement with ruby/furigana, pitch/color, and consistent baselines where data is available.
- Covered surfaces include feed filter chips, mini-guide/sidebar labels, topbar create/search controls, tooltip-visible labels when appropriate, and other stable YouTube UI text containing Japanese.
- Controls remain passive: clicking YouTube chips, nav links, buttons, search fields, menus, and tooltips preserves native YouTube behavior.
- Avoid enhancing duplicate hidden/tooltip-only labels in a way that double-speaks, double-renders, or creates nested duplicated text.
- Do not automatically OCR thumbnails while adding chrome coverage.
- Preserve YouTube performance and avoid black screens/content shift from scanning virtualized surfaces.
- Add/update YouTube smoke tests with DOM fixtures for filter chips, mini-guide labels, and the `作成` topbar button, asserting reader classes/ruby appear without breaking native click dispatch.
- Coordinate with P0-01, P0-14, P0-16, and P0-24 because this touches passive interactive elements and YouTube scanner scheduling.
- Push to `main`, publish/trigger a release, and verify in signed-in YouTube that chrome controls are enhanced.

## P0-26 Furigana/Pitch Enrichment Fetch Pipeline Is Too Serial

Source evidence:
- User reports major performance issues in how furigana is pulled down.
- User expectation: furigana/pitch/enrichment requests should use concurrent batches.
- Related symptoms include YouTube text remaining unenhanced or delayed, newtab search/page chrome missing pitch, and Immersion Kit interactions taking seconds when a reparse/enrichment path is triggered.

Acceptance:
- Audit the enrichment pipeline for Japanese parses, furigana, pitch accent, JPDB/Jiten/local source lookups, and page-text enhancement.
- Replace purely serial per-token/per-node fetch loops with bounded concurrent batches.
- Preserve ordering and deterministic rendering: batched results must map back to the correct text nodes/tokens without race-condition flicker.
- Use request de-duplication and caching so repeated visible words across YouTube chips, captions, settings, search results, and dictionary content do not refetch identical data.
- Keep concurrency bounded per source to avoid rate limits, UI jank, and browser throttling.
- Render progressively where safe: visible text should get immediate fallback styling and upgrade as enrichment arrives, without blocking clicks or scrolling.
- Add unit tests for batching/de-duplication/order preservation and at least one browser/performance smoke that proves a large visible batch is not fetched strictly one-by-one.
- Coordinate with P0-08, P0-14, P0-19, P0-20, P0-23, P0-24, and P0-25 because this is shared infrastructure.
- Push to `main`, publish/trigger a release, and verify user-visible pages feel instant rather than waiting on serial enrichment.

## P0-27 Immersion Kit Source Label Stretched By Enhancement/Layout

Source evidence:
- User screenshot: `/var/folders/pw/w51pz2xd16z4x521kb7m7x3r0000gq/T/codex-clipboard-400756e9-e6b4-4edc-a7bb-544af4099c15.png`.
- In the dictionary popover/source accordion, the label that should read `イマージョンキット`/`Immersion Kit` is stretched/split across the row as `イ   マージ   ョ   ン   キット`.
- The same screenshot shows source accordion rows such as `翻訳`, `文法`, and an Immersion Kit row while a YouTube page is visible behind the popover.

Acceptance:
- Reproduce the stretched Immersion Kit label in the popover/source list, including on YouTube if that is where the layout trigger occurs.
- Identify whether the root cause is ruby/token wrapping, flex/grid spacing, `justify-content`, letter spacing, source-title enhancement, or an accordion header layout rule.
- Source labels remain compact, readable, and consistently aligned with other source rows after ruby/pitch enhancement.
- Preserve source row click/toggle behavior and native popover scrolling.
- Add a DOM/CSS regression test or visual smoke for Immersion Kit source headers in popovers with Japanese enhancement enabled.
- Coordinate with P0-03, P0-08, P0-19, and P0-25 because this touches Immersion Kit and enhanced passive labels.
- Push to `main`, publish/trigger a release, and verify the label no longer stretches.

## P0-28 Dictionary Popover Action Pills Do Not Navigate

Source evidence:
- User screenshot: `/var/folders/pw/w51pz2xd16z4x521kb7m7x3r0000gq/T/codex-clipboard-2fe0a85c-df0c-46b1-bf89-f4c2fb7de9bc.png`.
- User reports clicking dictionary popover pills does nothing.
- Example visible pills: `Jiten`, `JPDB`, `Jisho`, `コピー`, `Yomu`, `BCCWJ #15281`, `Jiten #20374`, and `JPDBv2の #18689`.
- Expected behavior: clicking `Jiten` should navigate/open Jiten for the word; equivalent external/source/action pills should navigate/open/copy according to their label and settings.
- This overlaps P0-16 for YouTube-specific action pills but is broader: dictionary popover action pills in the app/popover itself are not opening targets.

Acceptance:
- Reproduce from a dictionary popover on at least one normal page and one YouTube page.
- External action pills (`Jiten`, `JPDB`, `Jisho`, `Yomu`) open or navigate to the expected URL in the appropriate tab/window context.
- Copy pills copy and show visible success/failure feedback.
- Source-detail pills such as corpus/source IDs open their intended detail pages or are rendered non-actionable if no target exists, never as dead clickable controls.
- Action handlers must not rely on inline event handlers or page-injected scripts blocked by CSP.
- Passive scanner wrapping must not swallow click events on pills or replace the actionable element with an inert wrapper.
- Add a focused smoke test that clicks each configured pill type and asserts the expected navigation/copy/action dispatch.
- Coordinate with P0-04 and P0-16 because both cover result/action pill plumbing.
- Push to `main`, publish/trigger a release, and verify the hosted/released popover pills work.

## P0-29 Newtab Study Word Mode Stuck Looking For More Words

Source evidence:
- User screenshot: `/var/folders/pw/w51pz2xd16z4x521kb7m7x3r0000gq/T/codex-clipboard-513eb8a5-f720-4a20-9fdb-171552411f5f.png`.
- URL: `https://hrussellzfac023.github.io/yomu-reader/newtab/index.html?q=%E8%AA%AD%E3%81%BF%E5%8F%96%E3%82%8B`.
- The Word tab shows prompt `よむ` and remains stuck on `Looking for more words...`.
- This contradicts the earlier P0-17 matrix line that P0-10 was `verified fixed` on current `main`; treat the live hosted flow as reopened until verified.

Acceptance:
- Reproduce the stuck Word tab state on the hosted `読み取る` URL and a local built equivalent.
- Word study must not get stuck indefinitely when a search query is present.
- If no more words are available from the active source/deck, show an actionable empty state and let the user switch source/deck or start a fallback/keyless session.
- Keyless study should work without JPDB keys, Jiten keys, Anki, or imported dictionaries.
- Preserve Kanji tab behavior and verify both Word and Kanji study tabs.
- Add a study-mode smoke test for `読み取る` and `よむ` that fails if the tab remains in `Looking for more words...` beyond a short timeout.
- Coordinate with P0-10 and P0-17 because this is a reopened study-mode regression.
- Push to `main`, publish/trigger a release, and verify the hosted/released page no longer gets stuck.
