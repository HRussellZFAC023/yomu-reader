# Yomu Refactor Backlog

Last updated: 2026-06-12 (done items removed per user direction — full history lives in CHANGELOG.md and git. New companion doc: `docs/study-hub-parity.md` holds the study-hub gap analysis and SH-1…SH-8 tickets, which are the current feature focus.)

## User Testing 2026-06-12 evening (wave 7, iPad Safari on the latest CI release — user: "add all the bugs I made notes on to the backlog and fix all of them"; 15 annotated screenshots, all mapped below — jpdb kanji-used, watch lookup pitch, prev-subtitle keyless, lines-panel gap, keyless completeness, copy UX, end screen, home feed, shortcuts settings, よね lookup, study settings, sidebar-left watch, paused OCR, comments, reddit)

- DONE 0.6.171 — UT-64 (root cause: our scanner annotated the kanji-used glyph link; JMdict lists 穏か as a rare しずか form, so the "furigana" was a wrong-word match. Glyph + pitch diagram now in the skip selector; regression test jpdb-widget-skip.test.ts.) Original: on jpdb.io vocab pages the kanji-component reading (e.g. しずか under 穏) renders BELOW the glyph instead of above it as ruby — match jpdb's native placement (annotation: "furigana position vs getting positioned below for Kanji used on jpdb.io").
- UT-65 lookup pitch accent gaps: the lookup-header pitch graph is sometimes missing entirely — seen on a conjugated phrase (気づかせてくれた, YouTube watch) and on reddit (賛成). Also the よね entry shows ONE pitch variant where all variants should be listed ("we should see all pitch variants"). Audit: deinflection→pitch resolution, keyless path, and multi-variant rendering in the header graph.
- UT-66 keyless subtitle annotation completeness: with NO API key, many kanji in subtitle lines are missing pitch + furigana ruby, some lines take seconds to annotate ("we expect all to be annotated instantly"), and after pressing Previous-subtitle the re-shown line loses its pitch info. Local-dictionary path must annotate fully + instantly and survive prev/next re-renders.
- DONE 0.6.174 — UT-67 (root cause: literal `&nbsp;` cues in auto-translated tracks render as blank 76px rows — entity decode added in normalizeCaptionText, empty cues now drop; verified in a Playwright panel probe + unit tests). Original: a gap renders at the bottom of the list that should not exist (last row sits half-cut below it; circled at 5:31). Check list height/inset math when the docked panel is bottom-positioned.
- DONE 0.6.174 — UT-68 (a) copy flash ✓ (b) subtitleCopyIncludeTranslation setting, default on ✓ (c) per-row eye toggle peeks the aligned translation ✓ — all three verified in the Playwright panel probe (jp-only copy confirmed with the setting off). Original: (a) pressing the per-line copy icon gives no feedback — add a subtle confirmation; (b) it currently copies BOTH jp and en — keep, but make "include translation" a setting; (c) when only jp lines are shown, add an eye icon per line to peek at the en translation.
- LIKELY FIXED 0.6.172 (UT-69) — end-screen tiles are ellipsis rows, covered by the UT-70 clamp sweep; confirm on-device after update. Original: our annotations clip the video-wall titles/view-counts shown after a video ends ("the text here is getting clipped") — treat end-screen cards as compact rows (color-only, no ruby) or fix their wrap.
- DONE 0.6.172 — UT-70 (ROOT CAUSE: scan-time layoutSensitive detection is correct, but hosts apply clamp styles AFTER annotation during hydration and rescans skip already-annotated words — stale ruby persisted forever. Fix: stripRubyInClampedRows sweep after every visible scan + 1.5s post-hydration pass; keeps span/color/lookup, removes only rt/rp. Unit tests clamped-ruby-sweep.test.ts; Playwright repro /tmp/clamp-race-repro.mjs verified the failure state and the recovery in the built bundle. UT-29/31 closed by the same sweep; verify on-device after update.) Original: home-feed titles truncate to bare "…" with wasted space; channel rows bad-wrap+clip (銭湯で「カポーン」…); マイクラ row missing text AND furigana; guide sidebar labels partially missing (とうろく); watch-page comment bodies vanish entirely (circled, @gigincho); video-wall/lockup metadata clipped. Fix the annotate-then-crop family for clamped rows once and verify on these exact surfaces.
- DONE 0.6.171 — UT-71 (Shortcuts panel now appends a fixed 'Study page keys' reference: Space/Enter, 1–9, U, ←/P, →/N, en+ja). Original: the Shortcuts panel must list ALL keyboard shortcuts available anywhere (reader app AND study/newtab page — Space reveal, digit grades, U undo, P previous, etc.), even if that means repeating bindings that are not currently rebindable.
- UT-72 custom-dictionary regression: user-installed jitendex dictionary entries no longer show in the lookup detail (よね) — "I installed jitendex dictionary (custom dictionary) but it is not showing here anymore". Find when imported-dictionary results stopped being merged into lookup details and restore.
- UT-73 lookup detail layout: grammar entries' spacing is inconsistent (ではない/じゃない vs です/だ rows indent differently); pitch underline missing on translation/grammar sentence lines; on reddit the whole panel has "weird styling" (section spacing collapses) — audit panel CSS isolation on host pages.
- DONE 0.6.171 — UT-74 (placebo 'Enable Yomu study page' checkbox removed — no runtime consumer; stored value preserved on save; address help now explains start/new-tab options incl. redirect extensions + iPad Home Screen and is no longer stomped by the offline help, which got rewritten to describe what the cache actually does). Original: "Enable Yomu study page" should read "Set as new tab" and it does nothing as a userscript on iPad Safari (no new-tab override) — detect unsupported managers and explain/hide; "Saves recent reviews for offline study" helper text is not useful — replace with something informative or drop it.
- UT-75 subtitle sidebar docked LEFT (UT-32 now CONFIRMED, iPad Safari): the player does not move next to the panel — the video area renders as a huge black void with the video absent ("VIDEO SHOULD BE HERE!! when sidebar is set to the left"). Reposition the player when docking left; verify on iPad-sized viewport.
- UT-76 unannotated chrome on watch/home: search box (検索), feed filter chips (提供:…/関連動画/おすすめ/最近アップ…), channel name + subscriber row under the title miss ruby/underline ("missing ruby and underline", "ruby missing") — extend coverage or consciously exempt and note why.
- UT-77 OCR follow-ups — (a)+(b) DONE 0.6.171 (snapshot pins to the contain-fit content box of videoWidth/Height, letterbox test added; OCR line clicks always preventDefault+stopPropagation — lookups run in document capture phase so they are unaffected). REMAINING (c) thumbnail credit/corner text OCR-eligibility. Original: (a) the paused-video OCR snapshot renders STRETCHED — preserve the frame's aspect ratio ("should keep aspect ratio"); (b) clicking OCR'd text on a thumbnail navigates to the video — it should open the lookup instead (swallow the click like subtitle words); (c) thumbnail credit/corner text (ノンクレジットOP) should also be OCR-eligible.
- UT-78 reddit host styling: lookup panel sections (TRANSLATION/MEANING/GRAMMAR) render with odd spacing/typography on reddit.com — host CSS bleeds into the panel; tighten the panel's style reset (overlaps UT-73, verify both).

## User Testing 2026-06-12 (NEW critical batch — fix first, in order; screenshots provided in session)

Study page (newtab):
- DONE 0.6.146 — UT-21 swipe-to-grade: root causes were (1) no `touch-action` on the study card, so iOS Safari claimed horizontal pans and fired pointercancel before the threshold (gesture could never complete on iPhone); (2) the "glows" were unrelated always-on decorations (review-mode accent-green edge glows at opacity 0.2 + a static green top strip duplicated in the boot HTML). Fixed: `touch-action: pan-y` + pointer capture once dragging; drag-gated edge glows (left red = fail, right green = pass) scaling with progress; all static glows removed. Verified with a mobile-viewport Playwright probe: rest = no glow, left drag = red glow + card transform, committed swipe submits the review (rating captured by mock).
- DONE 0.6.147 — UT-22 known-word furigana on study sentences: `renderNewTabSentenceHtml` now clamps furigana mode "all" to "known-status" for study-page sentences (front sentence + immersion examples), so known/mature/never-forget words render without ruby while unknown words keep it. Stricter explicit modes pass through. Regression tests in `new-tab-sentence-furigana.test.ts`; OCR/subtitle paths already honor the user's mode by design (their surfaces are reading aids, not review surfaces).
- UT-23 jpdb Learn queue parity — RECON DONE 2026-06-12 (signed-in profile clone, jpdb.io/learn + /settings + /review captured before connectivity dropped). FINDINGS: (1) Learn's "111 due" = 20 vocabulary + **91 KANJI** — kanji reviews are invisible to the vocabulary API entirely, so an API-only queue can never match Learn while kanji are due; surface kanji dues via the live-review bridge or show an explicit "+N kanji on jpdb" chip instead of silently diverging. (2) The user's study page showed "Due 0" while Learn says 20 due vocab — suspect due_at timezone handling (account timezone UTC+00:00) or deck scoping; once jpdb.io is reachable again, run `/tmp/jpdb-api-compare.mjs` (key cached at /tmp/.jpdb-user-key) to count api-due vs 20. (3) Queue-shaping settings ON for this account: pin-top-deck (pinned deck's cards first), enable-learning-period, auto-reveal-new-cards, learn-kanji/learn-subkanji, do-not-learn-basic/katakana-names/loanwords (these suppress NEW cards the API would otherwise serve — our 359-new count vs Learn's choices likely differs because of them). (4) API mechanics: list-user-decks rows are POSITIONAL TUPLES; 70 decks / 34,151 unique pairs union fine at concurrency 4; burst calls trigger connection-closed throttling — keep the 5000-pair chunking and add retries. ROOT CAUSE SHIPPED 0.6.156 (UT-56): the single-credential normalization (Jiten key wipes apiKey) made the study page mirror ONLY Jiten whenever both keys were entered — jpdb dues were invisible by design. Dual credentials now coexist (separate settings fields, queue merges both providers; dual-credential scenario added to the smoke). ALSO confirmed: lookup-vocabulary DOES return cards with states ['due'] and past due_at on this account (samples: 止まる/つまり/おやおや with due_at weeks in the past) — so the study page's "Due 0" is OUR counting/scoping bug (suspects: newTabDueSummary classification, deck scoping to a new-cards-only deck, due_at comparison clock). jpdb.io TLS-resets under bursty API use from this IP — space calls out (≥300ms) and retry whole runs. Kanji-due chip DONE 0.6.165: the review bridge now also runs on jpdb.io/learn, parses "N due items (V vocabulary and K kanji)" (unit-tested against the real Learn text) and the study page's due summary appends "+K kanji on jpdb.io" while a tab is bridged. Due-counting bug: superseded by the dual-credential fix (0.6.156) — re-verify on-device after the user updates. REMAINING (small): encode pin-top-deck + new-card suppression flags into queue composition if the user still sees order differences after updating.
- UT-24 SUGGESTION (user ask): improve newtab UI/layout/review loop — better use of space, cleaner layout, accessibility, ease of use, especially mobile. Treat as a design slice: audit spacing/hierarchy on a 390px viewport, propose + ship incremental improvements.

YouTube (verify live with Playwright MCP on the user's signed-in Chrome — user explicitly asked for this):
- UT-25 ruby + colorising still not available immediately after page load (long-standing; persists in 0.6.144 per user). REPRO NOTES 2026-06-12: anonymous search page ~0.7-0.8s to first annotation; SIGNED-IN home feed (profile-clone harness) 1.0-1.4s after script eval, in-viewport coverage complete after settling. The remaining gap vs the user's experience is userscript-manager boot timing on iPad Safari (script eval happens later than CDP injection). Treat as monitoring unless the user reports it again on 0.6.149+ with timings.
- PARTIAL 0.6.149 — UT-26 grid gaps: ROOT CAUSE found on the signed-in feed (profile-clone harness): shelf carousels hydrate items on PAGING, not visibility, so filtering collapsed rendered neighbours and pulled blank unhydrated `ytd-rich-item-renderer` slots (387px empty boxes) into view. Fixed: `syncUnrenderedYouTubeShelfSlots` keeps unhydrated shelf slots off-flow until YouTube fills them (test in youtube-filter.test.ts; live-verified: empty visible slots 9 → ~0 modulo sweep timing). Shelf backfill DONE 0.6.150: `backfillSparseShelves` pages a visible shelf with fewer than 3 unfiltered items via its "show more" (is-truncated shelves, `div#dismissible ytd-button-renderer button` — live-verified 5→9 hydrated) or next-arrow control, max 4 pages per shelf, 1.5s throttle.
- UT-27 OCR coverage: run OCR on (a) video thumbnails and (b) the paused video frame; clear those OCR overlays when playback resumes. SCOPING 2026-06-12: thumbnails are already eligible in principle (default provider google-lens is keyless+always configured, `ocrMinImageArea` 45000 < typical thumbnail area, no ignore rule matches) — so (a) needs live-feed debugging of why candidates don't surface (hover-trigger vs auto-scan gating in `canAutoScanImage`?). (b) DONE 0.6.148 — paused-video frames: `ImageOcrController` snapshots a visible paused video into a pinned `.jpdb-ocr-video-frame` image that flows through the normal image OCR pipeline, and releases it on play/emptied/destroy (capture-phase media listeners; data-URL results excluded from the cache; occlusion check exempts the snapshot; `ocrVideoPauseFrames` setting, default on, no form UI yet). Unit tests in `ocr-video-frames.test.ts`; browser-verified on a live YouTube watch page (snapshot pinned at the player rect on pause, removed on resume). Note: the probe's GM fetch shim cannot binary-decode Google Lens responses, so end-to-end line rendering was verified only as far as the shared pipeline — real userscript managers provide proper binary transport. (a) thumbnails: still open, needs live-feed debugging of `canAutoScanImage` gating on the signed-in MCP browser.
- UT-28 tokenizer compound miss: 学曲 in ASR subtitles parsed as two single-kanji tokens (学 + 曲) instead of one word (likely ASR rendering of 楽曲); lookup shows single 曲. Prefer longest-match compounds in subtitle/lookup parsing even for OOV/ASR-typo strings; check whether jpdb parse returns the split and we can re-merge via local dictionary lookahead.
- UT-29 text disappearing on YouTube: channel-name/metadata rows render empty or as bare "…" (three circled instances on home grid + watch page). ANALYSIS 2026-06-12: in the user's screenshots the ruby line is visible while the BASE text is missing — i.e. ruby applied inside a single-line clamped row crops the base line (same root cause family as UT-31). The scanner's `fragileByCompactLayout` (dom/index.ts) rejects ≤12-char nowrap rows <180px wide outright (channel names: 15/21 unannotated in anonymous repro), but the user's feed shows those rows ANNOTATED WITH RUBY — so the signed-in feed path annotates them via a route that misses the `layoutSensitive` color-only classification. Find that route on the live feed (needs MCP browser), then make compact metadata rows annotate color-only (`allowRuby: false`) instead of being skipped or ruby'd.
- UT-30 many UI elements never get colourised/ruby: watch-page video title, description body, related-video titles, native caption overlay lines (circled in screenshots). PARTIAL REPRO 2026-06-12: anonymous chromium watch page annotates title, description and 105 related-video words fine — desktop watch coverage is OK. Remaining suspects: the NATIVE caption overlay lines (we deliberately may skip them?) and the signed-in/iPad feed surfaces. Re-verify on MCP browser.
- UT-31 annotated text cutoff/overflow: text with furigana no longer fits its container and gets clipped (e.g. related-video channel line "Claude新モデル…" cut off mid-glyph). Shares the UT-29 root cause (ruby inside single-line clamped rows). Anonymous repro found 0 clipped ruby rows on the search page — the failing surface is the signed-in feed/iPad layout. Fix together with UT-29 (color-only for compact clamped rows).
- UT-32 subtitle side-panel layout: when the Yomu subtitle panel docks LEFT, the video/player is not re-aligned properly. NOT REPRODUCED 2026-06-12 in chromium at 1380x900 NOR 1024x1366 (iPad portrait): panel docks left, player shifts right with zero overlap, layout correct (probe `/tmp/yt-watch-probe.mjs`, screenshots taken). The user's surface is Safari/iPad (Userscripts app) — NEEDS INFO: confirm browser/orientation/theater-mode, retest on 0.6.147+.
- UT-33 (spotted in screenshots, not called out): with the docked subtitle panel open, page content at the bottom edge can sit beneath the panel/player chrome (bottom row partially obscured in 06:59 screenshot). Verify panel inset/scroll padding when docked.

Profiling evidence 2026-06-12 (user reports of empty slots + loading shift, keyless ask): with the CURRENT build, layout-shift profiling with attribution (probe /tmp/yt-cls-probe.mjs) measured total CLS 0.024 on desktop signed-in home and 0.036 on emulated m.youtube — every notable shift attributed to YouTube's own chrome (footer, filter chip bar), ZERO shifts from filtering, ZERO lingering empty shelf slots, identical keyed vs keyless. Keyless newtab journeys verified (Space reveal, next, search mode, connect CTA). The on-device reports almost certainly come from the installed pre-0.6.149 userscript — ASK USER to update and re-observe; if still present, capture device+page specifics. Watch item: ytLockupMetadataViewModelTextContainer micro-shift (0.002) could be our metadata annotation — pre-reserving line-height is available if it grows.

Late additions 2026-06-12 (second feedback wave, user messages during session):
- DONE 0.6.151 — UT-34 shortcut hints: hints were already hidden on coarse pointers; now the first used shortcut (Space reveal or digit grade) sets a persisted `keyHintsDismissed` UI-state flag that hides all `.jpdb-reader-newtab-key-hint` pills for good. BONUS BUG FOUND+FIXED while verifying: the study keydown listener was root-scoped, but focus sits on body after load and falls back after every re-render — Space/digit shortcuts were effectively dead on most visits; the listener now binds at document level (page is always Yomu's own; input/search/settings targets filtered). Browser-verified: Space reveals from fresh load, hint pill gone after first use. REMAINING (small): audit subtitle panel/popup surfaces for other always-on shortcut labels.
- DONE 0.6.151 — UT-35 swipe-to-grade toggle: new `newTabSwipeReviews` setting (default on) in Settings → Study, gates `canSwipeCurrentStudyCard`; en+ja labels.
- UT-36 look-and-feel consistency pass (user ask): refine the overall look and feel; make every surface consistent with Yomu styling/UX (spacing, typography, button shapes, accent usage, dark/light parity). Treat as an audit slice producing concrete diffs per surface.
- DONE 2026-06-12 — UT-37 simulated user testing: six personas walked through their journeys (transcribed below; informed by this session's live harness observations). Each numbered finding is filed as its own item:
  - Persona "commute reviewer" (iPhone, one-handed): "Swiping finally works, but after I grade with a button the next card just snaps in — when I swipe it animates. Feels inconsistent." → UT-45. "The deck picker and status pill are at the top, I can't reach them with my thumb." → folded into UT-24/UT-43.
  - Persona "jpdb power user" (desktop, keyboard-only): "Space/digits work now. But there is no key for UNDO — I fat-finger 'easy' and have to grab the mouse." → UT-40. "The queue still isn't my Learn order." → UT-23 (open).
  - Persona "first-time installer": "I installed it, opened the study tab and got random 'practice words' with no explanation why — I hadn't connected anything. Took me a while to find the API key field in a giant settings list." → UT-41 (first-run connect flow + CTA on the practice-words notice), UT-42 (settings dialog information architecture: grouping + search).
  - Persona "Jiten learner": "Undo and the batch breather are great. I want my Jiten study decks scoped on the study page like JPDB decks are." → UT-44.
  - Persona "iPad immersion watcher": "Feed is clean now, but when filtering kicks in the page still jumps under my finger" → UT-39 (open). "Some channel names get ruby and then the row crops" → UT-29/31 (open).
  - Persona "Anki student": "The adapter chip is good. On the study page I'd like to see per-deck due counts before I start." → UT-46.
- DONE 0.6.167 — UT-40/57/58/59 undo + navigation: undo now covers EVERY provider (Jiten reverses server-side; JPDB/Anki re-queue locally with an honest "review still counts upstream" toast and a session-counter walk-back); U key and the undo button show for all; pressing Previous right after grading IS the undo gesture; every study card has a stable #card= URL pushed into history (reload restores the card; browser back walks the session and undoes across a grade boundary). ALL live-verified on the seeded Jiten account: URL per card, reload restore, back navigation, Previous-undo returning the graded card.
- DONE 0.6.161 — UT-41 first-run connect CTA: unconfigured practice state renders a "Connect JPDB / Jiten / Anki" pill in the count slot that opens settings (note: the fallback notice itself only renders for CONFIGURED sources, so the CTA keys off the practice pool + no-credentials state). Browser-verified keyless: renders, opens the dialog. Possible polish: deep-link to the API panel section.
- CLOSED (already shipped) — UT-42 settings IA: the dialog already has section tabs AND a settings search box with an empty state (`data-settings-search`, bindSettingsSearch, activateSettingsPanelWithoutClearingSearch) — discovered while starting the slice; nothing to add. If the user still finds it a wall, the next lever is per-section descriptions or a "most used" pinned section.
- UT-43 one-handed reach — MEASURED 2026-06-12 (390×844): status pill at y=522, deck pickers 574-676, controls bar fixed at 774; an ~86-98px dead band sits between the cluster and the controls. Attempts to stretch the cluster down via shell height:100dvh + fr rows + align-content (even INLINE) did not move it — the grid's middle row resolves to content size (104px/533.5px/0px) for a reason that needs a real devtools inspect (suspect: an intermediate wrapper or align-content interplay). NEXT: inspect live, then either flex-ify the study column (margin-top:auto on the cluster works in flex) or move status/deck INTO the controls bar on phones. Safe-areas already honored by the controls bar.
- DONE 0.6.164 — UT-44 Jiten deck scoping: JitenApiClient.listStudyDecks + studyDeckWordKeys; picker lists "Jiten · name" options (`jiten:(id)` values, shown for Jiten-only creds too); picking one scopes the session to the deck via word-key intersection (jpdb deck picks now scope OUT the Jiten batch; 'all' merges). LIVE-VERIFIED on the seeded account: scoped queue = exactly the 10 deck words, the out-of-deck due card excluded, counts correct (Left 10 · Due 0).
- DONE 0.6.162 — UT-45 grade transitions: `playCardEnterTransition` runs after every post-grade advance (buttons, failed-requeue redisplay) — 170ms enter motion matching the swipe feel, disabled under prefers-reduced-motion.
- DONE 0.6.160 — UT-46 Anki per-deck due counts: getDeckStats (one call, verified live: review_count+learn_count per deck) with a findCards fallback, 60s cache, rendered as "Deck · N" in the picker.
- DONE 0.6.152 — UT-38 playlist stacks: reproduced on the signed-in feed — lockup mix cards (ミックスリスト…) have collection thumbnails + watch?v=…&list=RD… links, so the old playlist heuristics saw a normal video link and the Japanese title passed the filter. `isYouTubePlaylistLikeCard` now treats collection/stacked thumbnails and list=RD radio links as always-hidden non-video items (test in youtube-filter.test.ts; live-verified zero visible mix stacks). If the user ALSO meant playlist stacks on the Yomu study page itself, re-open with a screenshot.
- DONE 0.6.163 — UT-39 mobile content shift: ROOT CAUSE — the anchor gated on window.scrollY>0 and compensated via window.scrollBy, but m.youtube.com scrolls inside a container, so the compensation NEVER engaged on mobile. `feedScrollerFor`/`feedHasScrolled` now walk to the actual scrollable ancestor and adjust its scrollTop (window fallback). Desktop regression-checked on the signed feed (filter counts unchanged, zero clipped rows). If shift is still felt on-device after updating, next step is batching collapses per animation frame.
- NOTE for user-visible gaps: the blank-shelf-slot fix shipped in 0.6.149 and shelf backfill in 0.6.150 — the device userscript must be UPDATED to see it; "still seeing gaps" reports from 0.6.144-era installs are expected.

Third feedback wave 2026-06-12 (goal directive; work until this file is empty):
- DONE 0.6.153 — UT-47 appearance presets: `appearancePreset` select (default / no-colors / new-only / underline-new / furi-all / furi-known-hidden / furi-hover / furi-off) maps onto wordColorStates + furiganaMode + furiganaHiddenStateGroups + the existing word color sources, with a live sample-sentence preview (data-settings-preview-lookup exempts it from the dialog's unwrapReaderWords pass — THREE call sites needed the descendant exclusion: form.ts, main.ts, newtab/runtime.ts). Hiding is per state group via root classes `yomu-furi-hide-(group)`; hover mode renders ruby + CSS visibility. Default furiganaMode changed 'all' → 'auto'. Browser-verified on the newtab settings dialog: presets drive the controls, preview restyles live (known rt display:none vs new rt visible; hover hides at rest). Preset/option ja localization DONE 0.6.162. REMAINING (polish): consider exposing underline thickness.
- PARTIAL 0.6.154 — UT-48 subs caching: refresh-persistence DONE — parsed cue html persists to sessionStorage (hashed settings+text key, 6h TTL, quota-safe) and restores without re-parsing (regression test in subtitles-controller.test.ts; keyless provisional-final results persist too since they have no authoritative tier). Cache keying audited: keys = parse-source signature + furigana/colour settings + text — provisional→authoritative gives at most TWO renders per cue with a key (by design), ONE keyless. REMAINING observation work: live-verify renders-per-cue on a real video (instrument renderSerial) and chase the "cache applied to other elements" report — transcript-row application is key-scoped, so a repro (which element got wrong html?) is needed from the user or the live harness.
- PARTIAL 0.6.158 — UT-49 Anki card layout: ROOT CAUSE of the RTK "kanji twice" found on the user's real RRTK deck — the template renders the glyph in FOUR @font-face fonts (YUMIN/YUGOTHB/HGRKK/KanjiStrokeOrders) that can't load in Yomu, so identical copies stacked up. `pruneRedundantAnkiGlyphRepeats` keeps one short Japanese glyph run per rendered side and strips dead tts elements (tests + real-deck verification). REMAINING: broader space-use/header audit of the rendered-card chrome (titles, deck labels) — sweep with the realistic fixtures.
- PARTIAL 0.6.158 — UT-50 AnkiConnect latency: PROFILED live (12k cards): cardsInfo ≈110ms/card (renders templates!), notesInfo/cardsToNotes/getDecks/findCards = milliseconds. The status-index rebuild no longer uses cardsInfo at all (fast path: cardsToNotes + getDecks + state-bucket findCards incl. is:learn is:review for relearning; reps/lapses default 0 in the index, exact values still load on per-note lookups). Verified ZERO state/deck mismatches vs the rendering path on 300 real cards; ~20min rebuild → seconds. Queue streaming DONE 0.6.159: getDecks pre-filter before cardsInfo + 40-card streamed chunks with early exit at limit×1.25 reviewable (pin updated in the paging test — the tail chunk is no longer rendered). REMAINING (optional): true lazy hydration where the queue accepts unhydrated cards and renders details on approach — only worth it if startup is still felt after 0.6.159.
- DONE 2026-06-12 — UT-51 multi-site sweep (probe /tmp/yt-multisite.mjs; NHK Easy, NHK News, ja.wikipedia, note.com, Aozora): all five annotate (138–3,741 words), ZERO page errors, ZERO clipped ruby rows, ZERO collapsed ruby bases, and page heights stable within ±55px (note.com's growth was its own lazy loading). Also confirmed the page scanner is colour-only on arbitrary sites BY DESIGN (ruby:0 pre-dates UT-52 — verified by running the pre-UT-52 build); furigana appears in lookups/subtitles/site-specific surfaces. No new issues filed from this pass; rerun after major scanner changes.
- DONE 0.6.157 — UT-52 scan exclusions: the geometry-based fragile rules (compact layout, tight typography, inline-control shape) and media-bearing text links (channel avatar + name) moved from REJECT to colour-only annotation (layoutSensitive); only genuine UI chrome (UI class names, icon-only links, control shapes) still skips. Live-verified on the search page: word count up, channel-name coverage materially up, zero overflowing/clipped ruby rows. This also structurally prevents the UT-29/31 crop family (ruby is never added to clamped compact rows).
- DONE 0.6.155 — UT-53 shorts full fill: backfill target = the shelf's own elements-per-row (clamped 3..8); fully-filtered shelves are paged too (they were hidden before backfill could run — catch-22 fixed). Live-verified on the signed feed: 4 pages clicked, 10→18 items hydrated; a shelf with zero Japanese shorts collapses entirely rather than showing a stub.
- DONE 0.6.155 — UT-54 mobile hints: `newTabKeyHintsRenderable()` (matchMedia pointer:coarse) gates kbd rendering for the Space hint and grade-digit hints at RENDER time; the CSS rule remains as a belt for hybrid devices.
- DONE 2026-06-12 — UT-55 queue mirroring (Jiten live-verified): seeded the Jiten test account via API (study deck "Yomu E2E Seed" id 2864, 10 common words batch-added; reader/parse body is {text: string[]}; create returns userStudyDeckId). Live probe (/tmp/jiten-mirror-probe.mjs — real key, GM bridge passing api.jiten.moe through): the study page's queue order matched srs/study-batch EXACTLY for all 11 cards (due card first, then server-ordered new cards) and the count line matched srs/due-summary (Due 1 · Left 11 · 10 new). Anki: scheduler-order parity is pinned by the due-timing unit tests + live per-deck due counts (0.6.160). Failed-requeue edge covered in the smoke. NOTE for probes: card transitions need ~1s before reading the prompt — 350ms reads race and look like duplicated cards.

Fourth feedback wave 2026-06-12 (user message):
- UT-57 undo for ALL providers (user: "U undoes the last review (Jiten) — why not on ALL"): jpdb-api and Anki have NO server-side undo (AnkiConnect cannot revert answerCards; jpdb API has no undo endpoint). Best-effort design: local re-queue undo — restore the graded card to the queue front, mark "review already recorded upstream" in the status line; for jpdb-live, drive jpdb.io's own undo through the review bridge (the /review page has an undo control — verify selector live).
- UT-58 back/Previous as undo: pressing Previous (or browser Back) right after grading should perform the undo (provider-aware per UT-57) instead of mere navigation.
- UT-59 stable URLs per study entry: each card gets a stable, shareable URL (e.g. /newtab/#card=vid:sid or jiten:wordId:readingIndex) pushed into history as the session advances, so browser back/forward walks the session and a reload lands on the same card. Combine with UT-58: back = undo when the previous entry was a grade.

Fifth feedback wave 2026-06-12 (user message):
- DONE 0.6.170 — UT-60 dual-key per-provider grade targets: queue dedupe now keeps the Jiten identity (jitenWordId/jitenReadingIndex) when the jpdb twin wins primary; reviewTargetsForNewTabCard offers jpdb-api and jiten-api independently (no else-chain); the EXISTING grade-target selector shows Both/JPDB/Jiten(/Anki) with combined labels ('Grades JPDB + Jiten[ + Anki card]'); no new chrome. Unit-tested (new-tab-card-selection.test.ts) + dual-credential smoke green.
- DONE 0.6.169 — UT-61 either/or sweep: all activeApiCredentialLabel/singleApiCredentialValue consumers replaced with both-aware logic (`combinedApiCredentialLabel` → "JPDB + Jiten"); hasJitenOnlyApiCredentials/isJitenApiActive mean exactly what they say; live-review preemption is now explicit-mode-only ('live-review' wins always; 'auto' with dual keys prefers the merged queues — the preemption test pins this). No setting silently changes behavior because another key was added.

Sixth feedback wave 2026-06-12 (user message):
- DONE 0.6.168 — UT-62 provider toggle: when both keys exist the deck picker gains "JPDB" and "Jiten" umbrella entries (provider:jpdb / provider:jiten) that scope the queue to one provider's full pool; "All vocabulary" merges, per-deck entries scope further. No new chrome.
- UT-63 all-provider status — API FINDINGS 2026-06-12: Anki already co-displays (separate meta item). Cross-provider jpdb⇄jiten states are NOT knowable synchronously: JpdbVocabularyInfo carries no card state, and the id spaces differ (jpdb vid ≠ jiten wordId), so each cross state needs one lazy per-expansion call — jiten reader/parse of the spelling returns vocabulary rows WITH knownState (verified shape earlier); jpdb side resolves via parse-by-spelling then card state. DESIGN: on detail expansion, fire both cross-lookups lazily (cached per word), append "Jiten X"/"JPDB X" chips as they resolve. Colour stays primary-state (user OK).

Harness note 2026-06-12: when the MCP signed-in Chrome is locked by a parallel session, CLONE the profile (`rsync -a --exclude='*Cache*' ~/Library/Caches/ms-playwright-mcp/mcp-chrome-7994bba/ /tmp/yomu-signed-profile/`, drop Singleton*) and drive it with `chromium.launchPersistentContext('/tmp/yomu-signed-profile', { channel: 'chrome', headless: false })` — cookies decrypt via the same Chrome keychain, giving the real signed-in YouTube/jpdb/jiten feeds. Signed-in audit script: `/tmp/yt-signed-audit.mjs` pattern (GM shim init + CDP Runtime.evaluate injection of dist files served on :8742).

Process notes for this batch: verify every fix live with Playwright (MCP signed-in Chrome for YouTube/jpdb; local smoke harness for newtab), screenshot before/after, release per slice, and keep adding newly spotted issues here — these are exactly the kind of issues testing should catch proactively.

## User Testing 2026-06-11 (critical batch — fix first, in order; ask for screenshots in follow-up notes for any non-repro)

Reader/typography:
- UT-8 furigana obstruction: the screenshot instance (channel-shelf bio) is fixed by the UT-4 clamp in 0.6.127. NEEDS INFO for other sites: ask user where else text gets obstructed (page + screenshot).
- UT-9 OCR ruby sometimes missing: not reproduced yet. NEEDS INFO: ask for a screenshot + the image/page where ruby was missing.
- UT-10 図書 としょ+しょ double furigana over empty box: screenshot crop doesn't identify the surface (popup? study card? kanji tab vocab list?). NEEDS INFO: ask which screen and for an uncropped screenshot.
- UT-11 keyless underline delay (colorisation only after tapping sentence): not reproduced; provisional/keyless parse paths all push a render on completion. NEEDS INFO: ask which surface (ASB subs? page text?) and whether video was paused.

Community intel: `docs/community-intel-backlog.md` (captured 2026-06-11) seeds the long-tail items — treat as backlog source after the batch above.

## Remaining Large Lanes (next sessions)

- Study-hub parity: work through `docs/study-hub-parity.md` SH-1…SH-8 (stats table, due summary, card browser with filters/search/bulk actions, review back fidelity, front audit, deck management, today panel/forecast, shortcut audit).
- P1 engineering: incremental Anki status-index refresh by mod-time; adapter state machine for nonstandard decks; Anki media manifest/card-audio cache; abortable visible-work scheduler.
- DONE 0.6.174 — ADR-0003 Kanji/Study follow-up: the popup render layer (rtk-info, jpdb-kanji-info, kanji-origin, kanji-practice, origin-graph-interactions) now lives in the `kanji-study` companion with the clients, core calls through the `popup/render.ts`/`yomuKanjiStudyCompanion()` seam, and page/newtab kanji drilldowns show the install-companion notice when the split library is absent. Release verifier now fails if concrete kanji client/render implementations leak back into `dist/yomu.user.js`.
- DONE 0.6.174 — ADR-0003 Anki render split: Anki-specific render sections (`renderAnkiActionRow`, existing-note section, new-card preview, rendered-card study body, glyph pruning) now live in `yomu-anki.user.js`; core keeps only the registry proxy plus a tiny shared review-button fallback so JPDB/Jiten grade controls still render if the companion is absent. Release verifier now fails if the concrete Anki render implementations leak back into `dist/yomu.user.js`.
- Userscript size context: ADR 0003 extraction remains active, but the internal target is cleared again — 0.6.174 is 1,831,318 bytes after the Anki render split (168,682 bytes of Greasy Fork headroom; 18,682 bytes under the 1.85 MB internal target). Run `npm run size:greasyfork-plan` before the next feature batch; it currently recommends no immediate extraction batch.
- Snow Leopard quality lane — PROGRESS 0.6.137: timer audit across the tree found three persistent intervals; (1) the new study-page session clock now STOPS outside Word-tab study (was ticking forever and over-counting goal time on stats/search tabs — fixed); (2) jpdb-review-bridge 12s heartbeat is load-bearing (consumers' staleness clock) and scoped to jpdb.io review tabs — OK; (3) preferred-site-language geolocation shim 60s interval only arms if a page calls watchPosition — OK. Subtitle controller rAF usages are all one-shot frames, no loops; visible-page scanner is event-driven (one drain timeout). 0.6.126's identical-render skip already removed the biggest battery drain (per-tick subtitle DOM rebuilds). MutationObserver audit note 2026-06-12: youtube.ts observer is loop-free — mutations inside [data-jpdb-reader-root] (shelf/bar both marked) are skipped, the attributeFilter (href/title/aria-label/is-in-first-column) excludes every attribute we mutate (class, data-yomu-*, aria-hidden), and the grid rebalance converges because removeAttribute on an absent attribute emits no record. Scroll listener passive + bottom-gated. Fallow dead-code back to ZERO findings 0.6.139 (stale suppression removed, ADR-0003 seam types/dup-exports documented + rules.duplicate-exports off with rationale in .fallowrc.jsonc, three post-refactor internal helpers privatized).

## JIT Subtitle Parse Contract (pinned 0.6.67)

- Playback-simulation regression tests pin the just-in-time guarantees in `tests/reader/subtitles-controller.test.ts`: continuous playback never reaches a cue that is not already parsed/cached (40-cue walk with realistic 30ms batch latency), and a long seek re-warms the active cue plus the 10-cue lookahead within one warmup turn.
- DOM-caption fallback (YouTube native captions) parses during the 180ms stability window instead of after it.
- By design: cues whose parse yields no annotatable words live in a TTL'd empty cache and re-parse periodically; token-bearing cues cache permanently until pruned.

## Current Scoreboard

- `npm run typecheck`, `npm run test:ci`, `npm run build`, `npm run docs:build`, `npm run verify`: all green at 0.6.100 (every release today gated by `npm run check`).
- `dist/yomu.user.js`: 1,831,318 bytes at 0.6.174 (168,682 Greasy Fork headroom; 18,682 under the 1.85 MB internal target) — Anki render split restored target headroom.
- Live e2e: the user's signed-in Playwright MCP Chrome is available (YouTube, jpdb.io, jiten.moe, claude.ai as of 2026-06-11); injection recipe in `.playwright-mcp/inject-youtube.mjs` / `inject-generic.mjs` (serve dist on :8742, GM shim init script, CDP `Runtime.evaluate` with `allowUnsafeEvalBlockedByCSP`, companions before core).
- Current user direction: verify journeys live, fix real bugs, keep this backlog groomed, then study-hub parity (`docs/study-hub-parity.md`); keep mobile/iPad users in mind.

## Reference Parity Tickets (open)

- DONE 0.6.174 — adapter state machine: the Anki status line now carries an explicit lifecycle chip + data-anki-adapter-state (disabled → probing → unreachable/connected → scanning → suggested/stale/ready; mobile-handoff reads ready), scan results list each field mapping with its confidence (high match / fuzzy match / unmapped), and saved mappings that reference fields missing from the current model surface as a pending stale/review-needed state before the form repairs them with live suggestions. Prepare untouched.
- DONE (verified 0.6.174) — Anki media manifest/card-audio cache: rendered-card image media hydrates through `retrieveMediaFile`, rendered-card audio stays lazy so long files do not block card details, media playback uses `mediaFileDataUrl`, and repeated filename requests reuse a bounded promise cache with failure eviction. Pinned by `tests/reader/jpdb.test.ts` rendered-media cases and `tests/reader/anki.test.ts` cache/retry cases.
- P1 PARTIAL 0.6.122 — Incremental Anki status-index refresh: the count gate now runs an edited-card mod-time sweep (`edited:N` findCards + `cardsModTime`, asbplayer pattern) whenever the collection count matches, so same-count edits/reviews done in Anki itself dirty the index and refresh through the proven rebuild path; sweep failure (AnkiConnect without cardsModTime) falls back to the count gate exactly as before (4 tests). Remaining (optional): replace the full rebuild with a surgical per-changed-card entry merge for very large collections.
- PARTIAL 0.6.144 — abortable visible-work scheduler: the visible-page scanner now (1) carries a scan generation — a newer scan request aborts the in-flight one between batches so fresh regions never wait behind stale ones; (2) filters stale targets at PARSE time (node gone / text changed), not just apply time; (3) caps parse batches by text volume (6k chars) so huge paragraphs can't stall a turn. Regression tests in visible-page-scanner.test.ts (18). The destroyed/apply-time guards + per-turn yields already existed. REMAINING (optional): a persistent IntersectionObserver registry to replace collect-at-scan-time entirely, and the settings-save/mobile-scroll smoke.

## Open Product Tickets

- SHADOW-DOM LANE (audited 2026-06-12, user ask: "good compatibility with web frameworks, shadow DOM and re-renders"): light-DOM framework re-renders already recover (mutation observer → 450ms rescan → fresh nodes re-annotate; pinned by visible-page-scanner.test.ts "re-annotates text after a framework re-render"), and word events are now shadow-retarget-safe via composedPath (pointerEventElement in main.ts, shipped 0.6.175). REMAINING (real feature work, ship as one slice): (1) text collection does not traverse open shadow roots (visibleTextWalker/TreeWalker stops at shadow boundaries; only youtube.ts crosses shadow for its own controls); (2) the auto-scan MutationObserver observes document.body only — mutations inside shadow roots are invisible; (3) reader styles are a document-level `<style>` — annotating inside a shadow root needs per-root style adoption (adoptedStyleSheets + fallback) or the ruby/colors render unstyled. Partial support would annotate words that then render unstyled, so do all three together.
- SUGGESTION (new, from allowlist review): a per-site "disable Yomu on this site" toggle (FAB menu + settings list) for sites where injection misbehaves — lighter than an allowlist, matches the userscript model.
- DONE 0.6.177 — deck-based word styling parity: JPDB, Jiten, and Anki rendered words now stamp shared deck membership classes/data plus provider-specific `jpdb-*`, `jiten-*`, and `anki-*` deck classes; merged Anki deck metadata keeps Anki styling even when JPDB/Jiten is the primary source. Pinned by rendered-word, Jiten token rendering, card-state signal, and JPDB generated shard coverage.
- Watch (post-0.6.70/0.6.77): YouTube ghost-card placeholders during heavy backfill — continuation pacing; grid fixes shipped 0.6.77 and live-verified, keep an eye on backfill stalls.

## P0: Current Product Gate

- Keep `npm run typecheck` green after every refactor batch.
- Keep Fallow dead-code at 0; drive complexity findings back to 0 before calling a Fallow pass done.
- Keep the build reviewable: no identifier minification, no obfuscation, no whitespace compactor, no remote executable loader.
- Do not claim Greasy Fork readiness until `npm run build && node scripts/sync-docs-userscript.cjs && npm run verify` passes.

## P1: Greasy Fork Size Strategy (ADR 0003)

- Use `npm run size:greasyfork-plan` after each build-affecting change; `dist/greasyfork-size-plan.json` holds companion budgets.
- First extraction batch after the 0.6.174 Anki render split: none required immediately; next large extraction candidate remains the Anki service/status-index surface if feature growth eats the restored headroom.
- Policy-safe remote JSON/CSS data packs for i18n copy, config metadata, non-critical styles; packaged fallbacks first, cached, digest-checked; remote JSON is data only, remote CSS is style only.
- Library URLs must be in `package.json` `yomu.allowedRequireUrls` before the verifier accepts an `@require`.

## P2: Hotspot / File-Length Work

- `src/reader/app/main.ts`: extract text lookup/token orchestration around `lookupRenderedSelection` / `showTextLookupResult`.
- IN PROGRESS 0.6.136 — newtab/controller.ts: search-result renderers (word/kanji result cards, summary meta, state labels) extracted to src/reader/newtab/search-view.ts as pure helpers with a NewTabSearchViewContext. REMAINING: the stateful detail-expansion cluster (renderSearchWordDetail/Definitions/KanjiSection) and review-target helper extraction.
- `src/reader/dom.ts`: extract text-target discovery, sentence/context extraction, token application, typography heuristics.
- `src/reader/settings/form.ts`: localization/help-link DOM relabeling helpers around `localizeSettingsForm`.
- `src/reader/settings/dialog-controller.ts`: split panel event wiring and async refresh helpers.

## P3: Duplication

- `tests/reader/jpdb.test.ts`: large clone group; extract fixtures carefully.
- Avoid broad edits to `tests/reader/new-tab-review.test.ts` until the new-tab controller settles.

## P4: Library Replacement

- Keep `fflate` (native `DecompressionStream` tried first; ~13 KB rendered isn't the problem).
- Focus size effort on first-party feature boundaries, not rewrites of maintained libraries.
