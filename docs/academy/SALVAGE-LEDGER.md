# Academy salvage ledger

`KEEP` means the artifact may be copied only with provenance/path validation. `ADAPT` means preserve the behavior or contract behind current interfaces and tests; do not wholesale copy. `REJECT` means it must not enter runtime. The lossless source inventory is [`evidence/stage-0/inventory.json`](evidence/stage-0/inventory.json).

## Canonical Reader work

| Source | Verdict | Evidence and boundary |
| --- | --- | --- |
| Pre-Academy Reader/NHK dirty work | KEEP, protected | User-owned and unrelated to Academy. Retained unstaged plus safety stash. Its framework-mirror vertical slice is focused-test green; exploratory probes stay untracked and out of Academy commits. |

## Donor A — `c2ed0722fda6dd16ba14520d1cd1b60155fab7c3`

| Payload | Verdict | Evidence and boundary |
| --- | --- | --- |
| Entrance, blue-hour doors, Rie welcome, full-bleed study composition | ADAPT | Strong experience direction; reimplement against the typed shell rather than porting the giant/ad-hoc app state. |
| `public/academy/content/source-ledger`, worksheet packs, mappings, weeks, extraction research/scripts | KEEP then migrate | Best source corpus. Validate hashes/counts and migrate into immutable source + augmentation schemas before runtime use. |
| `codex-production-v1/backgrounds` | KEEP per asset | Direct OpenAI manifest; 26 plates remain individually subject to geography, safe-zone, mobile, and runtime-home review. |
| `codex-production-v1/cinematic-events` and `lesson-assets` | ADAPT per asset | Composition keepers; character likeness and source-answer-cue fidelity must pass before binding. |
| Approved Rie sprite | KEEP | Direct OpenAI transparent half-body and locked calibration anchor; copy with manifest entry and hash. |
| `quality-2`–`quality-5` protagonist references | KEEP as identity references | Runtime thumbnails/busts still need alpha/style normalization or OpenAI regeneration. `quality-1` is rejected. |
| `claude-production-v3`, `codex-production-v2/sprites`, generic `claude-production/sprites`, Python/chroma-key generation | REJECT | Wrong provenance/style and known identity drift; cannot enter runtime manifests. |
| Initial `src/academy/**` runtime | ADAPT selectively | Useful content/domain research, but the nine-lesson compression, god-object growth, duplicate study surfaces, and stale Reader copies are rejected. |
| Audio/private media and semantic cue research | ADAPT | Keep semantic theme/SFX mapping only. Private Persona/Shinday assets require explicit local/prototype rights state and never ship by path accident. |
| Worker/access code and upload scripts | ADAPT after security review | Use as evidence for routes/contracts; reconcile with current Cloudflare best practices and focused modules before deployment. |
| Broad deleted/generated docs state, logs, caches, screenshots, test outputs | REJECT | Donor is 4.8 GB with 271 dirty tracked and 2,529 untracked records; unrelated deletions and generated artifacts are not salvage. |

## Donor B — `666b339b6da643cddef9b9142488d987416021d7`

| Commit/payload | Verdict | Evidence and boundary |
| --- | --- | --- |
| Shared rebuilt base `1528bfb6c` shell, engine, stage, world, activity, journal/bond direction | ADAPT | Stronger modular seam, but multiple files exceed guardrails (`study-bridge` 1,222 lines, `curriculum` 1,659, `lessons-content` 2,092). Port deep interfaces and tests, not the tree wholesale. |
| `826572bb1` audio controller/catalog/preferences | ADAPT | Gesture/location concepts and tests are useful. Supersede with one four-bus `AudioDirector`, semantic slots, ducking, visibility, offline, and rights-safe manifests. |
| `b6bc25881` responsive stylesheet contract | ADAPT | Keep viewport/touch/layout invariants and test approach; rebuild CSS after annotation injection against final composition. |
| `18148240e` map CTA styling | ADAPT | Preserve action hierarchy and veil contrast only; final map must read as geography, not a card grid. |
| `054a057a8` onboarding/profile | ADAPT | Profile model/tests useful; 809-line onboarding and Lesson-0-only route are insufficient. Split steps and add manual/mock placement plus midstream bridges. |
| `ce9f058de` journal/cast contracts | ADAPT | Keep unlock, stars, replay, and read model; align all cast data with the dossier and three-bond requirement. |
| `4247b9de1` campus navigation | ADAPT | Typed direct navigation/availability is strong; reconcile area vocabulary, current objective, accessibility, and final map art. |
| `48acebeb7` direct app wiring | REJECT as a patch | Behavior is required, but direct additions to the app orchestrator contradict deep-module/file-size constraints. Rewire through small composition roots. |
| `666b339b6` due queue, two-way kanji, listening mechanics, validators | ADAPT | Keep mechanics/tests and current Reader repositories. Soya wording/media is evidence only until provenance/reuse audit. |
| Dirty `app.ts`, `academy.css`, week index, and week rail | ADAPT after isolated diff review | Do not copy until final shell composition and annotation-aware browser tests exist. |
| Dirty `sprites-out/**` v2 fleet | REJECT | Listed as low-quality/generated v2 output; excluded by the visual ledger. |

## Dormant Codex worktrees

| Worktree | Verdict | Evidence and boundary |
| --- | --- | --- |
| `0cdc` dynamic campus map | ADAPT | Strong typed `AreaId`, access hints, direct destinations, context/events, and tests. Map renderer grew by hundreds of lines; split model from DOM and reconcile with location-first composition. |
| `2f33` responsive CSS contract | ADAPT | Preserve 320px/tablet/wide invariants and contract tests; do not accept CSS without post-annotation real-app review. |
| `3df8` accessibility/E2E harness | KEEP then retarget | Adversarial no-skip release gate, axe, touch targets, overflow, screen-reader and viewport matrix are valuable. Update selectors/state to the final runtime and Browser evidence rules. |
| `4794` Cloudflare access | ADAPT inherited Worker; REJECT the claimed patch | The prompt/output records that no implementation occurred. Review the existing 1,568-line Worker and migration as donor evidence, then split progress/access/media with Cloudflare skills and targeted tests. |
| `47ad` study session/SRS/writing tray | ADAPT | Deterministic sound/text/speaking planner, repair-first queue, model-answer gating, and writing tray are strong. The 438-line store must become an adapter over the canonical learner event/SRS repositories, not a parallel store. |
| `6e4a` zero-to-N1 expansion | KEEP as planning evidence | Clear source boundaries and outcome map; not release-ready content or proof of coverage. Import only alongside machine mappings and provenance validation. |
| `9eef` worksheet-pack v2 | ADAPT requirements; REJECT prompt as implementation | Contains a scoped implementation brief but no completed patch. Rebuild the script against immutable source/media schemas. |
| `c067` PWA/offline | ADAPT | Keep shell/chapter cache separation, explicit downloads, bounded media, upgrades, and tests. Reconcile with current Vite paths and never precache the corpus/private OST. |
| `c091` KanjiVG/Doodle | ADAPT | Correct two-way recognition/production and canonical Reader Doodle/stroke imports. Move authored mnemonic data out of TS, add lifecycle cleanup/keyboard equivalent, and bind attempt/review events. |
| `c6ab` cast/journal | ADAPT | Tuple-enforced learning links, textbook legend label, stars/replay tests are useful. Complete dossier fidelity, three bond steps, expressions, and event-log projection. |
| `d543` onboarding/profile | ADAPT | Clean profile validation and tests; extend from a basic profile into fiction note, four approved portraits, all three start paths, privacy, and bridge state. |
| `dd1c` audio controller | ADAPT as primary AudioDirector candidate | Focused modules and rights-empty default catalog are stronger than the earlier monolith. Extend to four buses, lesson duck/resume, visibility/offline and exact semantic cue slots; retain silent fallback. |
| `e008` source audit | KEEP | Committed evidence with coverage matrix and machine-readable missing-week/media/listening backlogs. Migrate into the canonical resource ledger; large backlog is tracking evidence, not completion. |
| `ec63` first-term story | ADAPT structure; rewrite prose | Scene metadata, speaker derivation, memory callbacks and choice effects are useful. It covers only early N4 and contains placeholder/over-compressed beats, so it cannot satisfy the six-season narrative gate. |
