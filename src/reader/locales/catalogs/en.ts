// Owner: locale-en
import { defineLocaleCatalog, ENGLISH_FALLBACK_MESSAGES } from "../catalog";

export const enCatalog = defineLocaleCatalog(
  "en",
  "source-approved",
  ENGLISH_FALLBACK_MESSAGES,
);
